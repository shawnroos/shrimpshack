---
title: "feat: auto v0.2.0 — Recipes (packaged workflows for the loop engine)"
type: feat
status: active
date: 2026-05-23
deepened: 2026-05-23
origin: docs/brainstorms/2026-05-23-auto-v0.2.0-recipes-requirements.md
---

# feat: auto v0.2.0 — Recipes (packaged workflows for the loop engine)

## Overview

`auto` v0.2.0 introduces **recipes** — named, file-backed JSON declarations of workflow topology. A recipe describes the initial ledger the engine builds (which units exist, their `depends_on` graph, which phase each runs in, and what each invokes). The engine reads the recipe and builds the ledger; everything downstream of that (tick loop, dispatch, predicate evaluation, resume) runs unchanged for A1 and extended for non-default topologies.

v0.2.0 ships:

1. The **JSON recipe format** + a single validator used by both the engine loader and the authoring skill.
2. **Three built-in recipes** (A1/A2/A4) encoding workflows the user already runs manually. (A3, Build-First Feedback, is deferred to v0.2.1 — it alone drives ~40% of the engine surface; see KTD-14.)
3. A **three-tier registry** (workspace → global → built-in) with first-wins resolution.
4. A **picker** at `/auto` start via `AskUserQuestion`.
5. An **authoring skill** (`auto-author-recipe`) that compiles recipes from prose.
6. **Engine extensions**: R11 (multiple parallel plan-loops, serialized advance) ships in v0.2.0; R12 (non-default phase ordering) ships its field-reservation only — the runnable form is deferred to v0.2.1 with A3. The adapter contract re-locks once, adding `enumerate_plan_units`.

Behavioral guarantee: v0.1.x users see no behavioral change. A1 is the canonical encoding of v0.1.1's behavior — picking A1 in the picker (or accepting the default) produces a first-tick ledger byte-identical to v0.1.1's.

---

## Problem Frame

v0.1.1 ships exactly one workflow shape — plan → seam → work → exit-on-P3 — and the user already runs at least four distinct workflows manually using Claude primitives + popular plugins. v0.2.0 packages those workflows as recipes so `auto`'s durable ledger, fan-out, and resume can serve all four. The frame, from the brainstorm: ***`auto` is just packaging Claude primitives and popular workflow plugins.*** (see origin: `docs/brainstorms/2026-05-23-auto-v0.2.0-recipes-requirements.md`)

---

## Requirements Trace

- R1. Recipes declare initial ledger topology — units + `depends_on` + phase + per-unit metadata.
- R2. The built-in recipes ship in v0.2.0 and demonstrate the format's expressive range. **v0.2.0 ships THREE (A1/A2/A4); A3 is deferred to v0.2.1** (per KTD-14).
- R3. V1 ships declarative-only JSON. `python_hook` is a reserved schema field that the V1 engine does not load.
- R4. Format prioritizes agent-writability over human-writability.
- R5. Three-tier search path: workspace → global → built-in; deduped by `name`, first-wins.
- R6. Picker labels each recipe with its tier so users see what they're picking.
- R7. Bare `/auto <plan>` opens the picker via `AskUserQuestion`.
- R8. `--recipe <name>` skips the picker.
- R9. Plugin ships `auto-author-recipe` skill that compiles recipes from prose.
- R10. Skill supports both prose-elicitation and reverse-derive-from-completed-run entry points.
- R11. Engine permits N parallel plan-loops in one run.
- R12. Engine permits non-default phase ordering. **Partially deferred to v0.2.1** (per KTD-14): the recipe format RESERVES `phase_order`/`terminal_phase` fields (they parse), but the V1 validator REJECTS non-default values, so non-default phase ordering is not RUNNABLE in v0.2.0. The field reservation lands in V1; the runnable code path (A3, AE5, the two-field predicate carving) ships in v0.2.1.
- R13. v0.1.x behavior preserved exactly when picking A1 (or default).
- R14. `--adapter ce|native` continues to work; orthogonal to `--recipe`.

**Origin actors:** A1 (end-user / recipe author + runner), A2 (authoring skill), A3 (auto engine), A4 (picker).
**Origin flows:** F1 (first-time built-in pick), F2 (author from prose), F3 (workspace shadows global), F4 (`--recipe` flag bypass).
**Origin acceptance examples:** AE1 (default picker behavior; covers R7/R13), AE2 (workspace shadows global; covers R5/R6), AE3 (skill produces A4-shape from prose; covers R9/R10), AE4 (A2's parallel plan-loop ledger shape; covers R11). **AE5 (A3's non-default phase ordering; covers R12/R13) is DEFERRED to v0.2.1 with A3** (per KTD-14). A new A4 runtime integration test (A4's `plan_output_to_paired_builders` end-to-end; resolves adversarial F2) is added in its place — owned by U5b.

---

## Scope Boundaries

- Mid-run recipe switching is OUT.
- Recipe inheritance/diffing is OUT (no `extends: a1` semantics).
- Community/shared recipe registry is OUT (sharing happens via committing workspace recipes or copying files).
- GUI/web picker is OUT.
- Auto-recommending a recipe from plan content is OUT.
- Telemetry on recipe usage is OUT.

### Deferred to Follow-Up Work

- **Marketplace re-sync and PR to `shawnroos/shrimpshack`**: Per global CLAUDE.md, the marketplace publishing dance is a separate step done after merge. This plan delivers v0.2.0 in `~/projects/auto`; the marketplace PR + version bump (0.1.1 → 0.2.0) is the follow-up.
- **Backlog task #31** (NO_TICK_LOCK hatch unfenced/untested) — pre-existing v0.1.x P3 surfaced in `auto`'s final review. Not blocking v0.2.0; can land alongside or after.

---

## Context & Research

### Relevant Code and Patterns

- **The single change point for recipe→ledger:** `lib/auto.py:194` — `run()` calls `ledger.init_ledger(repo_root, run_id, adapter=adapter, units=[], loop_phase="plan")`. Today `units=[]` and `loop_phase="plan"` are hardcoded. Recipes replace those two argument values. `init_ledger` (`lib/ledger.py:489`) already accepts arbitrary `units` and `loop_phase`; the caller just never supplies them.
- **Phase grammar enforcement (the R12 surface):** Three load-bearing sites all key on the literal `"plan"` vs `"work"` distinction:
  - `lib/tick.py:533-562` — `_tick_body` 3-way phase switch
  - `lib/tick.py:634-671` — `_maybe_seam` hardcodes plan→seam(→work) transition
  - `lib/ledger.py:315-352` — `recompute_predicate` binary plan-vs-everything-else branch
  - Plus secondary touchpoints at `lib/tick.py:508` (predicate-met preempt) and `lib/tick.py:610` (post-advance recheck).
- **Plan-loop singularity (the R11 surface):** Enforced by three top-level scalars + one adapter call: `ledger.plan_step` and `ledger.exit_predicate_result.gaps_open` (scalars, one per run); `lib/tick.py:376 advance_plan_loop` calls `adapter.next_plan_step(ledger_dict)` which returns ONE step; today `units=[]` in plan phase so dependency machinery is dormant.
- **Engine seam (unchanged in v0.2.0):** `lib/orchestrator.py::dispatch_batch`, `ready_units`, `_is_ready`, `_dependency_satisfied`, `_ancestor_ids`, `converge`; `lib/ledger.py::unit_is_terminal`, `record_verdict`, `transition`, `_atomic_write`, `_with_locked_ledger`; `lib/tick.py::advance_work_loop`, `_ready_fix_unit`, `_ready_reenqueue_unit`, `detect_and_halt_stalled`. These read only `units[].state`/`depends_on`/`findings[]`/`adapter_scale` and stay topology-agnostic.
- **Commands→lib dispatch pattern:** Three commands today (`auto.md/auto-status.md/auto-resume.md`) each with NL routing prose in the .md and one canonical `bash "${CLAUDE_PLUGIN_ROOT}/lib/X.sh" "$ARGUMENTS"` dispatch line. The smoke test (`tests/smoke/scaffold.test.sh:87-109`) enforces this exactly-once invariant. The picker logic for R7 lives entirely in `commands/auto.md`'s NL routing prose — no new dispatch lines, no smoke-test changes.
- **Test harness:** `tests/run.sh` collects `tests/{smoke,unit,integration}/*.test.sh`. Drop a new `tests/unit/recipes.test.sh` and it auto-runs. Helpers at `tests/helpers/test-helpers.sh`. Pattern examples: `tests/unit/ledger.test.sh` (875 lines), `tests/unit/orchestrator.test.sh` (640 lines).
- **Plugin manifest auto-globs:** `.claude-plugin/plugin.json` declares `skills: "./skills"` and `commands: "./commands"`. Adding `skills/auto-author-recipe/SKILL.md` requires no manifest edit.
- **Contracts (locked, additive-only extension convention):** `docs/contracts/ledger-schema.md` already exercises the additive-extension pattern (the `attempt` field from Bug #6 — schema §2.3 documents `attempt: 0` default and notes "additive / backward-compatible — an old ledger with no `attempt` field reads as `0`"). v0.2.0 uses the same shape for all new fields.

### Institutional Learnings

- **`feedback_plan_documents_transition_code_doesnt_wire_it`** is the anchor learning for this plan. R11 and R12 are exactly the "prose-says-X, engine-must-now-honor-X" shape that bit v0.1.1 six different ways. Every recipe topology rule must be wired through a single centralized helper, not pattern-matched at each consumer. Allocate review-loop budget to post-build code review, not pre-build doc deepening.
- **`feedback_fix_the_class_not_the_cited_instance`**: grep the v0.1.1 engine for every site that branches on `loop_phase` (predicate, `_tick_body`, `_maybe_seam`, status reporting, resume). All sites route through the new phase-grammar helper. Round-1 fixing only cited sites is a known failure mode.
- **`reference_claude_loop_task_daemon_primitives`**: each background agent self-writes its verdict to the ledger atomically; A2's judge unit reads verdicts from disk, not from in-memory `backgroundTasks`. The same pattern applies to A4's comparator.
- **`feedback_slash_command_nl_routing_pattern`** + **`feedback_slash_command_arg_substitution`**: picker logic and `--recipe` flag parsing live in `commands/auto.md`'s NL routing prose; `lib/auto.sh` stays headless. The .md body keeps the exactly-one-`$ARGUMENTS`-line invariant.
- **`feedback_loop_monitor_terminal_state_field`**: A3 has two work-phases. Without a `terminal_phase` field, the predicate fires after the sketch work-phase and the run silently exits. Required schema field for any non-default `phase_order`.
- **`feedback_predicted_bugs_need_tests_not_conventions`** + **`feedback_deterministic_over_probabilistic_v1`**: validation is mechanical, not conventional. JSON Schema validator gates the recipe at load AND at skill-write time. **Accept the reserved `python_hook` field but ignore it; reject ALL OTHER unknown top-level fields.** (Per R3, `python_hook` is a future-compat reservation, not an error.) The "reject unknown fields" principle holds for everything outside the explicit reserved list.
- **`feedback_new_tests_need_deliberate_fail_smoke_check`**: every new test (AE1–AE4 + the A4 runtime test + schema validator tests + the V1-rejection test for non-default phase_order) gets a deliberate-fail check via the **Edit tool**, not a sed/regex revert. Particularly load-bearing for the A4 runtime test (the `(plan,work)` emitter-disambiguation could silently fire A1's emitter instead — adversarial F2) and the atomic-ordering test (A2 could silent-exit before work units emit). (AE5's sketch-fires-first deliberate-fail moves to v0.2.1 with A3.)
- **`feedback_subagent_write_verification`**: the authoring skill, after writing the recipe file, reads it back and runs the validator before reporting "saved." Same for any subagent-dispatched implementation units in this plan.
- **`feedback_review_loop_catches_narrowing`**: budget ≥3 code-review rounds. Claude-modes V2 took 9; claude-dispatch v0.1.1 took 3+.

### External References

None. Pure Python 3 stdlib + bash + on-disk JSON; no framework dependencies. The locked contracts in `docs/contracts/` are the canonical reference.

---

## Key Technical Decisions

- **KTD-1. A1 is a Python constant; `recipes/a1.json` is the user-facing override target.** *(Round-2 revision per adversarial F4 — the original "remove legacy path, A1 always from JSON" choice introduced a new runtime file dependency on every bare `/auto` invocation.)* The canonical A1 topology lives in `lib/recipes.py` as `A1_BUILTIN = {...}`. The picker and registry resolution still surface `recipes/a1.json` if present in workspace/global tiers (override path), but if absent at the built-in tier, the engine falls back to the constant. **R13 regression test** ("first-tick ledger from A1 recipe ≡ v0.1.1's first-tick ledger, modulo additive fields") still holds — it tests against whichever source resolves. **Trade:** one canonical source eliminates drift; built-in install corruption can't break bare `/auto`; user-facing override still works.
- **KTD-2. Base validator + optional lint pass.** The JSON Schema validator lives in `lib/recipes.py` as `validate(recipe_dict)`. The authoring skill calls a wrapping `validate_and_lint(recipe_dict)` that adds editorial checks the engine ignores: "phase declared in `phase_order` but no unit assigned to it," "depends_on creates an unreachable subgraph," "terminal_phase has no units assigned," **"workspace/global recipe description text matches a built-in's verbatim"** (added per security observation 1 — defense against description-spoofing). Both layers live in `lib/recipes.py`; the contract is `validate`, the editorial pass is `lint`. Skill output that passes `validate` is engine-acceptable; skill UX gets quality-error catching at write time.
- **KTD-3. Phase-grammar helper enforcement: AST lint catching the STRING CLASS, not just constant-subscript shape.** *(Strengthened per adversarial F5 — the round-1 design was regex-strength dressed as mechanical.)* Every site that today branches on `loop_phase` reads from `lib/phase-grammar.py::current_phase(ledger)`. The lint forbids the **string literal `"loop_phase"`** anywhere outside `lib/phase-grammar.py` (not just `ledger["loop_phase"]` subscripts). All identified bypasses (`_key = "loop_phase"; ledger[_key]`, dict iteration, `getattr`, `__getitem__`) require the string to exist somewhere — banning the string literal catches the class. Mechanical, not convention.
- **KTD-4. Plan-loop units in N>1 mode execute SERIALIZED. Adapter contract re-locks with new op `enumerate_plan_units`.** *(Round-2 revision per feasibility F4 — resolves both G4 AND the producer gap.)* The orchestrator advances exactly one plan-loop unit per tick (serialized round-robin) — so the existing `next_plan_step(ledger)` signature is preserved unchanged for that op. **A new adapter op `enumerate_plan_units(ledger) → list[unit_dict]` is added to the locked contract.** Both `ce` and `native` adapters implement it. The op runs when the actively-advancing plan unit reaches `plan-done`; it enumerates THAT unit's plan output (the op reads `ledger["loop_phase"]`-adjacent state to know which plan unit just completed — for A1 there is only one, so `(ledger)` suffices). The emitter `plan_output_to_work_units` reads this list via the adapter call, not from a ledger field. **Round-3 Gap A resolution (A2's multi-plan disambiguation):** the op signature stays `(ledger)`-only (no `unit_id` param — that's still v0.3.0). Instead, **each plan unit's `enumerate_plan_units` output is persisted onto that unit's `dispatch_context.enumerated_units` field when the unit hits `plan-done`** (written by the engine in the same advance tick, before the unit goes terminal). A2's `judge_winner_to_work_units` emitter then reads the winning plan unit's stored `dispatch_context.enumerated_units` directly from the ledger — no re-call of the op, no per-unit op param needed. This keeps the op signature honest AND gives A2 a deterministic source for the winner's work units. **This is the v0.2.0 adapter contract re-lock.** Round-robin tie-breaker: see KTD-7 / U6 — `pick_next_plan_unit_to_advance` picks the ready plan unit with the oldest per-unit `last_advanced_at` (null sorts oldest), ties broken by `units[]` declaration order.
- **KTD-5. Phase-transition emitters are an explicit named-function registry in `lib/emitters.py`; recipes reference them by name in a `phase_transitions` block.** (Resolves G1.) V1 ships **exactly 3 emitters** (A3 deferred per KTD-14 below, so the `review_findings_to_plan_input` emitter is also deferred): `plan_output_to_work_units` (A1 + A2's chosen-plan downstream), `judge_winner_to_work_units` (A2), `plan_output_to_paired_builders` (A4). **Round-3 Gap B resolution (where the emitter hint lives):** the recipe carries a top-level `phase_transitions` array — each entry is `{"from": <phase>, "to": <phase>, "emitter": <emitter-name>}`. This is a NAMED top-level schema field (U2 lists it; the validator accepts it, not rejects it as "unknown"). It disambiguates A1 vs A4 at the shared `(plan, work)` boundary (A1 → `plan_output_to_work_units`, A4 → `plan_output_to_paired_builders`). The emitter NAME must be one of the 3 V1-registered names — the validator rejects unregistered names (so a recipe can't name a v0.3.0 emitter that doesn't exist yet). The implicit-vs-explicit fork: V1 recipes reference the SHIPPED emitter set by name; v0.3.0 may let recipes register NEW emitter functions — that's the deferred "arbitrary emitter" feature, distinct from V1's "name one of ours."
- **KTD-6. Atomic ordering via new `transition_and_emit` ledger primitive.** *(Round-2 revision per feasibility F2 + adversarial F1 — round-1's "same `_with_locked_ledger` write" framing was implementable only with a NEW primitive that the plan never specified.)* The seam-handler does NOT call `record_verdict` then a second write for emission. Instead, the orchestrator detects (on the next tick) that a triggering unit transitioned (e.g., judge verdict-returned in a prior tick's `record_verdict`) and calls `ledger.transition_and_emit(emitter_fn, ...)` — a single new `_with_locked_ledger` body that reads ledger, calls emitter, appends pending units, recomputes predicate, atomic-writes. **Emitters MUST be pure functions** (signature `emit(ledger, triggering_unit) → list[new_unit_dict]`); they MUST NOT call `record_verdict`/`transition`/`set_loop` (re-entrancy: `_flock_run` opens a new fd each call → deadlock). The seam-handler owns the lock; the emitter only computes.
- **KTD-7. Per-unit `plan_step` and `gaps_open` for plan-phase units (R11).** A1's single plan-loop continues to use the top-level scalars for byte-identical compatibility. A recipe with N plan-loops uses per-unit fields. The `recompute_predicate` plan branch reads per-unit when present, scalar otherwise. **Combined with KTD-4's serialized advance, the per-unit fields are written one at a time (the actively-advancing unit's plan_step gets updated each tick).**
- **KTD-8. Single predicate field `met`, phase-aware.** *(Round-2 revision — KTD-6 predicate carving from round 1 was driven solely by A3's two work-phases. With A3 deferred per KTD-14, the carving is unnecessary.)* `met` retains today's meaning, extended to be aware of `phase_order` and `terminal_phase` (when present): in `loop_phase == "plan"`, `met` requires existing plan-rules; in `loop_phase == terminal_phase`, `met` requires existing work-rules. With A1/A2/A4 all using default `phase_order = ["plan", "seam", "work"]` and `terminal_phase = "work"`, the predicate's behavior is byte-identical to v0.1.1's for A1; A2's parallel plan-loops use the per-unit aggregation path from KTD-7. The two-field carving (`phase_met`) is **deferred to v0.2.1 with A3**.
- **KTD-9. Picker default: short-label options with one-line descriptions.** Multi-line ASCII previews via `AskUserQuestion`'s preview field land as the picker's full form **if** verified to render. Otherwise, the picker uses short labels and includes a hint "type `/auto --recipe a1` to skip the picker." A `/auto --show-topology a1` command (deferred to implementation) is the fallback surface for visual reference.
- **KTD-10. Single ASCII topology renderer at `lib/topology-render.py`, called by picker + authoring skill + auto-status.** The renderer takes `(recipe_dict, width_hint) → str`. `skills/auto-author-recipe/references/visual-vocabulary.md` is the renderer's contract doc, not a separate spec.
- **KTD-11. `recipe` and `phase_order` are top-level ledger fields.** Schema extension is additive: missing `recipe` → recipe-blind v0.1.x ledger (treated as A1); missing `phase_order` → default `["plan", "seam", "work"]`.
- **KTD-12. Resume re-reads the ledger, not the recipe file.** The ledger's `recipe` + `phase_order` fields carry forward all topology info; the recipe file is only read at `init_ledger` time. Resume across recipe file deletion is safe.
- **KTD-13. `--recipe` flag writes resolved tier to stderr when non-built-in.** *(Added per security finding 2 — supply-chain hijack via workspace recipe shadowing.)* When `resolve(name)` returns a non-built-in tier, the resolver emits `[auto] resolving 'a1' from workspace (.claude/auto/recipes/a1.json)` to stderr. CI logs show the override; interactive users in the picker already see the tier badge. Low cost, closes the silent-shadow attack surface.
- **KTD-14. A3 (Build-First Feedback) deferred to v0.2.1.** *(Round-2 revision per product-lens Finding 1 — A3 alone drives ~40% of v0.2.0's engine surface: predicate carving, `terminal_phase` requirement, `review_findings_to_plan_input` emitter, non-default `phase_order` support, AE5. With A3 deferred, v0.2.0 ships A1+A2+A4 + W (work-only) as the recipe-format proof points. The brainstorm's original "all four together" decision was made WITHOUT visibility into how concentrated A3's engine cost would be — that surfaced only at round-1 deepening.)* v0.2.1 gets its own brainstorm/plan/review cycle for A3 once R11's parallel-plan mechanics have proven out in the field. The recipe format reserves `phase_order` and `terminal_phase` fields as future-compat — they parse but the V1 validator REJECTS values other than the default for non-A3 recipes, EXCEPT the explicitly-supported work-only phase_order (see KTD-15). Tests against A3's specific shape (AE5, the two-field predicate, the `review_findings_to_plan_input` emitter) are removed from v0.2.0.
- **KTD-15. W (work-only) is a 4th v0.2.0 built-in recipe — `phase_order: ["work"]`, no plan phase.** *(Added 2026-05-25 from field feedback — see memory `feedback_auto_prepare_execute_operator_traps`. An agent operating `/auto` on an ALREADY-REVIEWED plan found the plan-loop re-derives finished work because `/auto` has no work-loop-only entry point. W is that entry point.)* W is the architecturally SIMPLEST recipe — it skips the plan-loop entirely rather than extending it. **How its units originate:** at `init_ledger`, the engine calls the adapter's `enumerate_plan_units(ledger)` op ONCE on the operator-provided plan/spec and loads the result as the initial `work`-phase units (state `pending`). This REUSES U5b's producer op — no new machinery; the producer serves both the plan-loop (mid-run emission) and W (init-time loading). No emitter, no plan phase, no `phase_transitions` block. `terminal_phase: "work"`. **Validator carve-out (U2):** `phase_order: ["work"]` is the ONE non-default value V1 accepts (alongside the default `["plan","seam","work"]`); everything else (A3's 4-phase grammar) still rejects. This is a deliberate, narrow exception — work-only is a tested code path, A3's grammar is not. **Run-met:** since `work` is both the only phase AND the terminal phase, the existing work-loop predicate applies unchanged (blockers==0, all_units_terminal, scale-aware). This is the recipe the v0.2.0 build session itself would have used (a converged hand-reviewed plan, implemented directly).

---

## Open Questions

### Resolved During Planning

- **Q1 — Ledger schema extension shape**: Additive top-level fields (`recipe`, `phase_order`, `terminal_phase`); additive per-unit fields (`phase`, `plan_step`, `gaps_open`, `dispatch_context`, `last_advanced_at`). All default such that v0.1.x ledgers are A1-equivalent. (`last_advanced_at` added per feasibility F5 — the round-robin tiebreaker discriminator; `dispatch_context` per feasibility F-B — the recipe-side `prompt_template` merge target.)
- **Q2 — Topology declarations for A1/A2/A4**: See Implementation Unit U7 for concrete JSON. (A3 deferred per KTD-14.)
- **Q3 — Adapter contract change for R11 (resolves the producer gap)**: **V1 re-locks the adapter contract** with one new op: `enumerate_plan_units(ledger) → list[unit_dict]` (per KTD-4 / feasibility F4). Plan-loop units in N>1 mode execute SERIALIZED — `next_plan_step(ledger)` signature is preserved; `enumerate_plan_units` is the producer the emitters read. Both `ce` and `native` adapters implement the new op. Concurrent plan advance (a `unit_id` parameter on `next_plan_step`) stays deferred to v0.3.0.
- **Q4 — Phase-aware exit predicate**: Single `met` field, phase-aware (per KTD-8). A1/A2/A4 all use default `phase_order`/`terminal_phase`, so the predicate is byte-identical to v0.1.1 for A1 and uses per-unit aggregation for A2. The two-field carving (`phase_met`) is **deferred to v0.2.1 with A3** (the only recipe that needed it).
- **Q5 — Picker on no-resolvable recipes**: `--recipe nonexistent` → exit 1 with resolved set printed. Built-in A1 absent → falls back to the `A1_BUILTIN` Python constant (per KTD-1); other built-ins absent → exit 1 with reinstall hint.
- **Q6 — Dynamic unit emission (G1)**: Explicit named-function registry in `lib/emitters.py`. V1 ships **3 emitters** (A3's `review_findings_to_plan_input` deferred per KTD-14): `plan_output_to_work_units`, `judge_winner_to_work_units`, `plan_output_to_paired_builders`. The engine maps `(from_phase, to_phase)` tuples to emitter names. See HLTD "Dynamic unit emission" section for the full table. The emitters read plan output via the new `enumerate_plan_units` adapter op (Q3).
- **Q7 — Atomic ordering via new ledger primitive (resolves G2/G3 implementability)**: A new `ledger.transition_and_emit(emitter_fn, ...)` primitive does the transition + emit + recompute + atomic-write in ONE `_with_locked_ledger` body (per KTD-6 / feasibility F2 / adversarial F1). Emitters are pure functions that MUST NOT call ledger mutators (re-entrancy / deadlock — `_flock_run` opens a new fd each call). The seam-handler owns the lock.
- **Q8 — `_bootstrap.py` shim does not exist; U5 must create it**: per feasibility F1 / repo-research F-A / adversarial F6, `lib/_bootstrap.py` today only has `load_ledger()`. Hyphenated modules (`adapter-ce.py`) are loaded inline in `lib/tick.py::resolve_adapter`. U5 adds a generic `load_lib_module(name)` to `_bootstrap.py` for `phase-grammar.py` (and reusable for `topology-render.py`). This is a real new shim, not a phantom one.

### Deferred to Implementation

- **The exact AskUserQuestion preview surface for ASCII multi-line content.** First implementation pass tries the preview field with multi-line ASCII; if rendering breaks, falls back to short labels per KTD-9. The fallback is the default behavior; the multi-line preview is the upgrade. Test both; ship whichever renders.
- **Exact prose-elicitation flow for the authoring skill (R9).** Sketched in U9; refined during implementation by running the skill against the three V1 built-in topologies (A1/A2/A4) as test cases (AE3 is the conformance test).
- **Whether the judge's verdict-write convention in A2 needs a new adapter op or can reuse `record_verdict`.** Initial pass: judge unit uses `record_verdict` like any other unit; its findings array carries the chosen winner (which the `judge_winner_to_work_units` emitter reads to know which plan's output to emit via `enumerate_plan_units`). If that's too restrictive in practice, surface as v0.3.0 work.
- **The exact `emits`/`emitter` field shape on recipe-declared units, when v0.3.0 lifts the implicit-emitter restriction.** V1 ships 3 hardcoded emitters with a per-phase-transition `emitter` hint disambiguating A1 vs A4; v0.3.0 may let recipes declare arbitrary custom emitters. The full contract for arbitrary emitters is deferred until a user-authored recipe needs one.

---

## Output Structure

The plan adds these new files. Modifies the rest in place.

```
~/projects/auto/
├── lib/
│   ├── recipes.py                    # NEW — loader, validator, 3-tier resolver
│   ├── phase-grammar.py              # NEW — centralized phase-decision helper (hyphen per repo convention; loaded via _bootstrap.py importlib shim like adapter-ce.py / adapter-native.py)
│   ├── topology-render.py            # NEW — single ASCII topology renderer; called by picker, authoring skill, and auto-status
│   ├── emitters.py                   # NEW — phase-transition emitter registry (G1) + transition_and_emit primitive (KTD-6); 3 emitters in V1
│   ├── recipes.py                    # NEW — loader/validator/resolver + A1_BUILTIN constant (KTD-1)
│   ├── auto.py                       # MODIFIED — --recipe flag, init_ledger call
│   ├── auto.sh                       # MODIFIED — pass through --recipe
│   ├── ledger.py                     # MODIFIED — schema extensions (additive) + transition_and_emit
│   ├── tick.py                       # MODIFIED — phase switch routes via helper; serialized N-plan advance
│   ├── _bootstrap.py                 # MODIFIED — add load_lib_module() shim for phase-grammar.py (F1)
│   └── auto-status.py                # MODIFIED — show recipe + phase grammar; surface stalled plan-unit
├── recipes/                          # NEW — directory for built-in recipes (V1: 3; a3 deferred to v0.2.1)
│   ├── a1.json                       # NEW — Classic CE Stack (also the A1_BUILTIN Python constant; JSON is override target)
│   ├── a2.json                       # NEW — Parallel Theories + Judge
│   ├── a4.json                       # NEW — Adversarial Pair + Comparator
│   └── schema.json                   # NEW — JSON Schema for validation
├── skills/
│   └── auto-author-recipe/
│       ├── SKILL.md                  # NEW — authoring skill (atomic mkstemp+rename write, F7)
│       └── references/
│           └── visual-vocabulary.md  # NEW — contract doc for lib/topology-render.py (KTD-10)
├── commands/
│   └── auto.md                       # MODIFIED — picker + --recipe NL routing
├── tests/
│   ├── unit/
│   │   ├── recipes.test.sh                 # NEW — loader, validator, resolver, A1_BUILTIN drift
│   │   ├── phase-grammar.test.sh           # NEW — helper edge cases
│   │   ├── phase-grammar-ast-lint.test.sh  # NEW — forbids "loop_phase" string literal outside phase-grammar.py (KTD-3)
│   │   ├── emitters.test.sh                # NEW — 3 emitter functions + edge cases
│   │   ├── topology-render.test.sh         # NEW — renderer conformance
│   │   ├── orchestrator.test.sh            # MODIFIED — round-robin (last_advanced_at) + stalled-exclusion tests
│   │   └── ledger.test.sh                  # MODIFIED — new field defaults + paused-at-seam v0.1.x fixture
│   └── integration/
│       ├── recipe-r13-regression.test.sh   # NEW — A1 ≡ v0.1.1 (owned by U4)
│       ├── recipe-picker.test.sh           # NEW — AE1, AE2 (owned by U8)
│       ├── recipe-authoring.test.sh        # NEW — AE3 (owned by U9)
│       ├── recipe-parallel-plans.test.sh   # NEW — AE4: A2 (owned by U5b)
│       └── recipe-adversarial-pair.test.sh # NEW — A4 runtime: paired-builders emitter fires (owned by U5b, resolves adversarial F2)
└── docs/
    └── contracts/
        ├── ledger-schema.md          # MODIFIED — schema extensions
        └── recipe-format.md          # NEW — recipe format contract (LOCKED day-zero for v0.2.0)
```

---

## High-Level Technical Design

> *This illustrates the intended approach and is directional guidance for review, not implementation specification. The implementing agent should treat it as context, not code to reproduce.*

### Recipe JSON shape (canonical)

A1 (Classic CE Stack) as the simplest concrete instance:

```jsonc
{
  "name": "a1",
  "version": "1",
  "description": "Classic CE Stack — plan → build → review → fix to P3-only exit",
  "phase_order": ["plan", "seam", "work"],
  "terminal_phase": "work",
  "default_adapter": "ce",
  "phase_transitions": [
    {"from": "plan", "to": "work", "emitter": "plan_output_to_work_units"}
  ],
  "units": [
    {
      "id": "plan",
      "phase": "plan",
      "depends_on": [],
      "invokes": {"adapter_op": "next_plan_step"}
    }
    // work units appear dynamically — the plan_output_to_work_units emitter
    // (named in phase_transitions above) emits them from enumerate_plan_units output
  ]
}
```

A2 (Parallel Theories + Judge):

```jsonc
{
  "name": "a2",
  "phase_order": ["plan", "seam", "work"],
  "terminal_phase": "work",
  "phase_transitions": [
    {"from": "plan", "to": "work", "emitter": "judge_winner_to_work_units"}
  ],
  "units": [
    {"id": "plan-1", "phase": "plan", "depends_on": [], "invokes": {"adapter_op": "next_plan_step"}},
    {"id": "plan-2", "phase": "plan", "depends_on": [], "invokes": {"adapter_op": "next_plan_step"}},
    {"id": "plan-3", "phase": "plan", "depends_on": [], "invokes": {"adapter_op": "next_plan_step"}},
    {"id": "judge",  "phase": "work", "depends_on": ["plan-1", "plan-2", "plan-3"],
     "invokes": {"adapter_op": "review", "prompt_template": "judge.md"}}
    // judge_winner_to_work_units reads the winner's stored dispatch_context.enumerated_units
    // (persisted when each plan unit hit plan-done) — no per-unit op param needed (Gap A)
  ]
}
```

A3 (Build-First) is **deferred to v0.2.1** per KTD-14 — it's the only recipe needing non-default `phase_order`, and it drove ~40% of v0.2.0's engine surface. Its sketch is preserved in the v0.2.1 brainstorm. The recipe format reserves `phase_order` and `terminal_phase` as future-compat fields; the V1 validator rejects non-default values so no untested phase-ordering code path ships.

A4 (Adversarial Pair + Comparator):

```jsonc
{
  "name": "a4",
  "phase_order": ["plan", "seam", "work"],
  "terminal_phase": "work",
  "phase_transitions": [
    {"from": "plan", "to": "work", "emitter": "plan_output_to_paired_builders"}
  ],
  "units": [
    {"id": "plan", "phase": "plan", "depends_on": [], "invokes": {"adapter_op": "next_plan_step"}}
    // plan_output_to_paired_builders (named above — distinct from A1's emitter at the
    // SAME (plan, work) boundary; this is the Gap-B disambiguation) emits:
    //   2 work units with dispatch_context.bias clarity|perf + 1 comparator depends_on both
  ]
}
```

**Note on `invokes` field shape.** Each unit's `invokes` dict carries `adapter_op` (one of the locked ops in `docs/contracts/adapter-contract.md` §2: `plan`, `deepen`, `review_plan`, `next_plan_step`, `do_unit`, `review`, plus the v0.2.0 re-lock addition `enumerate_plan_units`) plus optional recipe-side metadata like `prompt_template`. The recipe-side metadata IS represented on the ledger via the additive `units[].dispatch_context` field (per U1) — the recipe loader (`lib/recipes.py::unit_for`) merges `prompt_template` etc. into `dispatch_context` BEFORE dispatch, after path-bounding validation. The adapter reads `dispatch_context` via its existing `unit` parameter.

### Dynamic unit emission (G1 — phase-transition emitters)

The `// emitted dynamically` comments are the single most important wiring point in v0.2.0 — if hand-waved, A2/A4 ship as JSON that validates but silent-fails at runtime. This subsection is the explicit specification.

**The producer (resolves feasibility F4 — the gap that almost shipped):** v0.1.1 does NOT produce work units in code — `next_plan_step` returns one step token and the seam pauses for manual work-unit creation. So the emitters have no source data unless the adapter provides it. **v0.2.0 re-locks the adapter contract with `enumerate_plan_units(ledger) → list[unit_dict]`** (KTD-4). The plan-loop, on reaching `plan-done`, calls this op; the emitter reads its return value. Both `ce` and `native` adapters implement it.

**Decision: emitters are a named registry; the recipe selects one per phase-transition via its `phase_transitions` block** (KTD-5 / Round-3 Gap B). A new module `lib/emitters.py` owns a registry mapping emitter NAME → function. V1 ships exactly the **3** emitters the V1 built-ins need (A3's `review_findings_to_plan_input` deferred with A3 per KTD-14). The recipe's `phase_transitions` entry names which emitter fires at each `(from, to)` boundary — so there's no tuple-collision ambiguity (the A1-vs-A4 `(plan, work)` collision is resolved by each recipe naming its own emitter, not by the engine guessing from the tuple).

Phase-transition emitter table for the V1 built-ins (A1, A2, A4):

| Recipe | `phase_transitions` emitter | Trigger | New unit shape | Owning function |
|---|---|---|---|---|
| A1 | `plan_output_to_work_units` | plan-loop-met | `enumerate_plan_units` output → 1 unit per item | `lib/emitters.py::plan_output_to_work_units` |
| A2 | `judge_winner_to_work_units` | judge verdict-returned | reads the winner's stored `dispatch_context.enumerated_units` (Gap A) → work units | `lib/emitters.py::judge_winner_to_work_units` |
| A4 | `plan_output_to_paired_builders` | plan-loop-met | `enumerate_plan_units` output → 2 work units (clarity/perf bias via `dispatch_context.bias`) + 1 comparator `depends_on: both` | `lib/emitters.py::plan_output_to_paired_builders` |

The seam-handler reads the recipe's `phase_transitions` entry for the current `(from, to)` boundary, resolves the named emitter from the registry, and calls `ledger.transition_and_emit(emitter_fn, ...)` (the new primitive from KTD-6) which calls it, appends units, recomputes predicate, atomic-writes — all in one locked body. If the recipe names an emitter not in the registry, the validator (U2) already rejected the recipe at load; if a boundary has no `phase_transitions` entry where one is required, the engine raises (explicit beats silent-no-op).

**Test coverage requirement:** each of the 3 emitters gets a unit test in `tests/unit/emitters.test.sh` (happy-path + edge case). **PLUS** each recipe gets an end-to-end integration test asserting the RIGHT emitter fires for its boundary (resolves adversarial F2 — A4 must have its own runtime test, not just A1's emitter reused): AE4 covers A2, a new A4 integration test covers `plan_output_to_paired_builders`.

### Atomic snapshot ordering (G3 — emit-then-recompute)

A2's judge unit terminating with `blockers==0 AND majors==0` would trigger `all_units_terminal==true` AND `met==true` under the v0.1.1 rules, EVEN THOUGH the chosen plan's work units haven't emitted yet. Same shape as `feedback_loop_monitor_terminal_state_field`: the terminal-state field is the wrong fingerprint for runs that emit units dynamically.

**Decision: required ordering inside one atomic ledger write.**

1. Apply the triggering transition (e.g., judge unit `verdict-returned`)
2. Run the phase-transition emitter (writes new `pending` units into `units[]`)
3. Recompute `met` (now sees the post-emission unit set)
4. Atomic write of the new ledger

All four steps run inside ONE `_with_locked_ledger` body via the new `ledger.transition_and_emit(...)` primitive (KTD-6). If steps 2 and 3 swap, A2 silent-exits before work units appear. If step 2 isn't in the same atomic snapshot, a crash between transition and emission leaves the ledger in an exited-looking-but-incomplete state — un-resumable cleanly. **The primitive is what makes this atomic** — without it (the round-1 framing's mistake), `record_verdict` and the emit would be separate locked writes with a torn-state window between them (feasibility F2 / adversarial F1).

**New ledger I-1 corollary** (documented in U11's contract update): "Phase-transition emission writes new pending units in the same atomic snapshot as the verdict-observation that triggers it; predicate recomputation runs on the post-emission unit set." Parallel to today's atomic predicate freshness invariant.

**Deliberate-fail test:** Edit `transition_and_emit` to recompute predicate BEFORE running the emitter on an A2 ledger fixture. Assert the predicate fires `met=true` after judge (incorrect). Confirm RED of the AE4 integration test. Restore.

### Serialized N parallel plan-loops (resolves G4)

Plan-loop units in N>1 mode (A2's 3 plan units) execute SERIALIZED — one `adapter.next_plan_step` call per tick, round-robin across ready plan units. The `next_plan_step(ledger)` signature is preserved unchanged. The top-level `plan_step` reflects the actively-advancing unit's value during its tick; other plan units are `dispatched`-but-idle.

**Round-robin tiebreaker (resolves feasibility F5 / adversarial F9):** the discriminator is the new per-unit `last_advanced_at` field. `orchestrator.pick_next_plan_unit_to_advance(ledger)` returns the ready plan unit (state `dispatched`, phase `plan`) with the oldest `last_advanced_at` (null sorts oldest, breaking initial ties by `units[]` declaration order). State lives in the ledger, so resume continues round-robin correctly.

**Stalled plan unit (resolves adversarial F3):** if a plan unit goes `stalled`, the round-robin ready-set excludes it (state ≠ `dispatched`). The predicate's plan-phase aggregation excludes stalled units from the "all plan units satisfy" universe (analogous to `terminal-skip`), AND the judge's `depends_on` gate treats a stalled dependency as a hard block surfaced to the operator via `auto-status` — NOT a silent hang. U6 specifies the recovery path.

### Start phase propagation

- `init_ledger` sets `loop_phase=recipe["phase_order"][0]`. For all V1 recipes (A1/A2/A4) this is `"plan"` (unchanged from v0.1.x). A3's `"work_sketch"` start is deferred with A3, so V1 never starts in a non-plan phase — but the helper is written to support it (the engine MUST NOT hardcode "plan is first") so v0.2.1's A3 needs no engine change.
- `phase_grammar.current_phase(ledger)` reads `ledger["loop_phase"]`, NEVER `phase_order[0]` directly. Load-bearing for resume.

### The centralized phase-grammar helper

```python
# lib/phase-grammar.py — directional sketch (loaded via _bootstrap.py::load_lib_module — the shim U5 ADDS; it does not exist today)

def current_phase(ledger):
    """Returns ledger['loop_phase'], or 'plan' if recipe-blind. NEVER reads phase_order[0]."""

def phase_order(ledger):
    """Returns ledger['phase_order'], or ['plan', 'seam', 'work'] if recipe-blind."""

def terminal_phase(ledger):
    """Returns ledger['terminal_phase'], or 'work' if recipe-blind."""

def next_phase_after_met(ledger, current):
    """Returns the next phase in phase_order(ledger) after current,
    or None if current is the terminal phase."""

def is_terminal_phase(ledger, phase):
    return phase == terminal_phase(ledger)

# Used by: _tick_body's phase switch, the seam-handler, recompute_predicate,
#          _ready_fix_unit's phase-awareness, auto-status's reporting.
# All sites route THROUGH this helper. The AST lint (KTD-3) forbids the
# string literal "loop_phase" anywhere outside this module.
```

This is the **one centralized decision** per the institutional learning. A deliberate-fail test reverts `next_phase_after_met` and asserts an integration test goes red. (In V1, with all recipes using default phase_order, the helper's non-trivial paths are exercised by A2's plan→work transition; A3's mid-flow `plan` phase that would most stress it is deferred.)

### Three-tier registry resolution

```python
# lib/recipes.py — directional sketch

def resolve(name, repo_root):
    """
    Look up <name> in:
      1. workspace: <repo_root>/.claude/auto/recipes/<name>.json
      2. global:    ~/.claude/auto/recipes/<name>.json
      3. built-in:  <plugin_root>/recipes/<name>.json
    First match wins. Returns (recipe_dict, source_tier).
    """

def list_available(repo_root):
    """
    Returns [(name, source_tier), ...] deduped by name (first-wins),
    sorted: workspace recipes first, then global, then built-in.
    For the picker.
    """

def load_and_validate(name, repo_root):
    """resolve + jsonschema validate. Returns recipe_dict or raises."""
```

---

## Implementation Units

- U1. **Ledger schema extension (additive)**

  **Goal:** Add the new ledger fields needed for recipes (`recipe`, `phase_order`, `terminal_phase` at top-level; `units[].phase`, `units[].plan_step`, `units[].gaps_open` when present). All fields default such that absence reads as A1/v0.1.x-equivalent.

  **Requirements:** R1, R11, R12, R13.

  **Dependencies:** None — must land first.

  **Files:**
  - Modify: `lib/ledger.py` (init_ledger signature additions; _normalize_unit; schema constants `LOOP_PHASES` extension to allow recipe-declared phases)
  - Modify: `docs/contracts/ledger-schema.md` (additive sections — see Patterns to Follow for the `attempt` field precedent)
  - Test: `tests/unit/ledger.test.sh` (new assertions for defaults + backward-compat)

  **Approach:**
  - At top-level: `recipe` (optional dict with `name`, `source_tier`), `phase_order` (optional list, defaults to `["plan", "seam", "work"]`), `terminal_phase` (optional str, defaults to `"work"`).
  - Per-unit (additive, all optional with backward-compat defaults):
    - `phase` — when absent, plan-phase units default to `"plan"`, others to `"work"`.
    - `plan_step` — per-unit mirror of the existing top-level scalar (used only for N>1 plan-loops; A1 keeps using the top-level scalar). Reads as `null`.
    - `gaps_open` — per-unit (same). Reads as `null`.
    - `dispatch_context` — dict of recipe-side metadata merged from `invokes` (e.g. `prompt_template`, `bias`). Per feasibility F-B. Reads as `{}`.
    - `last_advanced_at` — timestamp the round-robin tiebreaker uses (feasibility F5). Reads as `null` (sorts oldest).
  - `LOOP_PHASES` constant extends to accept recipe-declared phase names. The validator (U2) enforces phase names are in `phase_order` AND (per KTD-14) rejects non-default `phase_order` values in V1 — A3's phase grammar parses but is not yet runnable.
  - Backward-compat invariant: an old v0.1.x ledger MUST read as a valid v0.2.0 ledger and behave identically (becomes implicit A1).

  **Execution note:** Test-first. Write the backward-compat tests (load an existing v0.1.x ledger fixture, assert it parses + the predicate evaluates identically) BEFORE adding any field defaults.

  **Patterns to follow:**
  - `attempt` field additive precedent at `docs/contracts/ledger-schema.md:130` (verified by repo-research) — exact prose pattern: "additive / backward-compatible — an old ledger with no `<field>` field reads as `<default>`."

  **Test scenarios:**
  - Happy path: New ledger written with no recipe field → reads back identically; predicate evaluates identically to v0.1.x.
  - Happy path: New ledger written with `recipe: {name: "a1", source_tier: "built-in"}` → reads back identically; predicate evaluates identically.
  - Edge case: A v0.1.x ledger fixture (commit-pinned) loads without error → predicate result matches the predicate result computed against the same ledger by v0.1.1 code (use git to fetch v0.1.1 ledger.py and run both predicates).
  - Edge case: Per-unit `plan_step` and top-level `plan_step` both present → engine reads per-unit (precedence rule documented).
  - Edge case: per-unit `last_advanced_at` absent → sorts as oldest (round-robin picks it first).
  - Error path: `phase_order` is non-list → init_ledger raises with clear error.
  - Error path: `terminal_phase` is not a member of `phase_order` → init_ledger raises.
  - Backward-compat (resolves adversarial F8): a v0.1.x ledger fixture with `loop_phase: "seam", seam_paused: true` (paused at seam) initializes/loads without error AND the predicate + phase-advance behave identically to v0.1.1 (resume-from-seam works through the new code path).

  **Verification:**
  - `tests/run.sh all` passes (184 existing + new ledger tests).
  - A v0.1.x-shaped ledger fixture loads, predicates identically, and round-trips through write→read unchanged.
  - The paused-at-seam v0.1.x fixture resumes cleanly.
  - `docs/contracts/ledger-schema.md` has the additive sections marked LOCKED.

---

- U2. **Recipe format + JSON Schema validator**

  **Goal:** Define the canonical recipe JSON shape; write the schema; implement a validator that runs at every load site (engine + authoring skill).

  **Requirements:** R1, R3, R4.

  **Dependencies:** U1 (ledger fields are the schema's eventual targets).

  **Files:**
  - Create: `recipes/schema.json` (JSON Schema 2020-12 draft, hand-rolled or jsonschema-library-compatible)
  - Create: `lib/recipes.py` (validator entry point; `validate(recipe_dict) → None | raises ValidationError`)
  - Create: `docs/contracts/recipe-format.md` (the locked day-zero contract)
  - Test: `tests/unit/recipes.test.sh`

  **Approach:**
  - JSON Schema 2020-12, validated using Python's `jsonschema` if already available, else a hand-rolled subset (test-driven, just enough to enforce the shape). Check if `jsonschema` is a stdlib-ish dependency we already have; if not, hand-roll.
  - Schema enforces: required top-level fields (`name`, `version`, `units`); optional `phase_order`/`terminal_phase`/`default_adapter`/`description`/`phase_transitions`; rejected unknown top-level fields EXCEPT the reserved `python_hook` (which parses but is recorded as reserved-not-loaded); each `units[]` entry has required `id`/`phase`/`depends_on`/`invokes`; `depends_on` references only existing unit IDs; `phase` values are members of `phase_order` (when `phase_order` is present).
  - **`phase_transitions` field (Round-3 Gap B / KTD-5).** Optional top-level array; each entry `{"from": <phase>, "to": <phase>, "emitter": <name>}`. The `emitter` value MUST be one of the 3 V1-registered emitter names (`plan_output_to_work_units`, `judge_winner_to_work_units`, `plan_output_to_paired_builders`) — the validator rejects unregistered names. This is what disambiguates A1 vs A4 at the shared `(plan, work)` boundary. A1/A2/A4 each carry a `phase_transitions` entry for their emitting boundary; the validator's conformance corpus (the 3 built-ins) exercises this.
  - **Security: `prompt_template` path bounding (security-lens Finding 1).** Any `prompt_template` value in `units[].invokes` MUST be a relative path with no `..` segments and no absolute path (no leading `/`). Schema enforces this via a pattern constraint AND `validate()` rejects any value that contains `..` or starts with `/` regardless of pattern coverage. Rationale: workspace recipes ship in committed code; without path bounding, a malicious workspace recipe could set `prompt_template: "../../../../etc/passwd"` and the adapter would forward sensitive file contents into LLM context. The `unit_for` projection in U3 is the second enforcement point — it re-validates before merging into `dispatch_context`.
  - The contract doc is the canonical reference. The schema enforces it mechanically.

  **Execution note:** Test-first. Write the conformance tests against the three V1 built-in JSON files (A1/A2/A4, fixtures from U7) before completing the validator. The three built-ins are the validator's conformance corpus.

  **Patterns to follow:**
  - `docs/contracts/ledger-schema.md` and `docs/contracts/adapter-contract.md` — the "LOCKED day-zero contract" framing, reading-test rule, severity-of-changes notes.

  **Test scenarios:**
  - Happy path: Each of A1/A2/A4/W JSON validates without error (U7 produces these fixtures; this test is the conformance corpus).
  - Edge case: A recipe with `phase_order: ["plan", "seam", "work"]` and no `terminal_phase` → validator infers default `"work"` and passes.
  - Work-only path (KTD-15): A recipe with `phase_order: ["work"]` and `terminal_phase: "work"` → validates (the ONE non-default phase_order V1 accepts — W is a tested path). No `phase_transitions` block required (W has no emitting boundary).
  - V1-rejection path (KTD-14): A recipe with any non-default `phase_order` OTHER than the work-only `["work"]` (e.g. A3's `["work_sketch", "review", "plan", "work_refine"]`) → validator REJECTS in V1 with a "non-default phase_order not yet supported (v0.2.1)" error. This guards against shipping untested phase-ordering code paths. v0.2.1 flips this to general acceptance.
  - Reserved field path: `python_hook` (the one explicitly reserved field per R3) → validator accepts but engine ignores. Any OTHER unknown top-level field → validator REJECTS with clear "unknown field" error.
  - `phase_transitions` path (Gap B): a recipe with `phase_transitions: [{"from": "plan", "to": "work", "emitter": "plan_output_to_work_units"}]` → validates. A recipe naming an UNREGISTERED emitter (e.g. `"emitter": "my_custom_thing"`) → REJECTS with "unknown emitter" error (V1 recipes can only name the 3 shipped emitters). A2 and A4 naming their respective distinct emitters at the same `(plan, work)` boundary → both validate (proves the disambiguation works).
  - Error path: `depends_on` references a non-existent unit ID → rejects with clear error.
  - Error path: A unit's `phase` is not in `phase_order` → rejects.
  - Error path: Missing required field (`name`, `units`, `units[].id`, etc.) → rejects.
  - **Security path** (Finding 1): `prompt_template` with `".."` segments (e.g., `"../../../etc/passwd"`) → rejects with clear traversal-blocked error. Absolute path (e.g., `"/etc/passwd"`) → rejects. Empty string → rejects. Valid relative path (e.g., `"prompts/judge.md"`) → accepts.
  - Deliberate-fail check: Edit-tool revert the V1-rejection rule (let non-default `phase_order` through) → run the V1-rejection test → assert it flips from REJECT to ACCEPT. Restore. Confirms the A3-deferral guard is mechanical.
  - **Security deliberate-fail check**: Edit-tool revert the `prompt_template` traversal rule (delete the `..` check) → run the security test case `prompt_template: "../../etc/passwd"` → assert it goes from REJECT to ACCEPT. Restore. Confirms the traversal mitigation is the load-bearing check, not convention.

  **Verification:**
  - `recipes/schema.json` validates each of A1/A2/A4 (the fixtures from U7) and REJECTS a non-default-`phase_order` recipe.
  - `docs/contracts/recipe-format.md` reads cleanly as a standalone contract per the contract-doc reading-test rule.

---

- U3. **Recipe loader + three-tier registry**

  **Goal:** Implement `resolve(name, repo_root)`, `list_available(repo_root)`, `load_and_validate(name, repo_root)` in `lib/recipes.py`. Workspace → global → built-in, first-wins, deduped by name.

  **Requirements:** R5, R6.

  **Dependencies:** U2 (validator must exist).

  **Files:**
  - Modify: `lib/recipes.py` (add the three resolver functions)
  - Test: `tests/unit/recipes.test.sh` (extend with resolver tests)

  **Approach:**
  - `resolve(name, repo_root)`: probe paths in order; first match wins; return `(recipe_dict, source_tier)`.
  - `list_available(repo_root)`: aggregate, dedupe by `name` (keep first), tag each with `source_tier`; sort workspace first, then global, then built-in (per F3).
  - `load_and_validate`: calls `resolve` then `validate`. Raises a single user-friendly error on validation failure.
  - `unit_for(recipe_unit_dict, recipe_dict) → ledger_unit_dict`: the recipe-unit-to-ledger-unit projection. Maps recipe fields (`id`, `phase`, `depends_on`, `invokes`) to ledger schema fields (`units[]` shape from `lib/ledger.py::_normalize_unit`). Merges `invokes.prompt_template` and similar recipe-side metadata into the ledger unit's runtime context (a new optional `dispatch_context` field on the ledger unit that the adapter reads). Pure function — input recipe dict + unit dict, output ledger unit dict.
  - `validate_and_lint(recipe_dict) → (errors, warnings)`: the skill-side wrapper around `validate`. Calls `validate` for hard errors (the contract), then runs editorial-only lint checks: phase declared in `phase_order` but no unit assigned; depends_on creating an unreachable subgraph; terminal_phase has no units assigned. Returns warnings separately so the skill can surface them to the user without rejecting the recipe. (Per KTD-2.)
  - Path probes use `os.path.join`, never string concat. Handle missing directories gracefully (workspace dir doesn't exist in a fresh repo).

  **Execution note:** Test-first against the AE2 scenario (workspace shadows global).

  **Patterns to follow:**
  - Existing config-path resolution in `lib/auto.py` for `<repo>/.claude/auto/` — same `git rev-parse --show-toplevel` discipline for repo root, no CWD-relative paths.

  **Test scenarios:**
  - Covers AE2: workspace recipe `<repo>/.claude/auto/recipes/team-review.json` exists alongside `~/.claude/auto/recipes/team-review.json`. `resolve("team-review", repo_root)` returns the workspace version, source_tier "workspace".
  - Happy path: `list_available` in a fresh repo returns exactly the three built-ins (a1/a2/a4), each tagged "built-in".
  - Happy path: `list_available` in a repo with a `my-flow.json` workspace recipe returns 4 entries: my-flow first ("workspace"), then a1/a2/a4 ("built-in").
  - Edge case: workspace recipes dir doesn't exist → no error; only global + built-in returned.
  - Edge case: global recipes dir doesn't exist → no error; only workspace + built-in returned.
  - Error path: `resolve("nonexistent", repo_root)` raises FileNotFoundError with the resolved search paths in the message.
  - Error path: Workspace recipe with invalid JSON → `load_and_validate` raises with file path in error.
  - Deliberate-fail: revert resolve's first-wins ordering (try built-in first) → AE2 test goes RED. Restore.

  **Verification:**
  - `tests/run.sh all` passes.
  - `lib/recipes.py` resolver covers all three tiers + missing dirs + the AE2 conformance.

---

- U4. **`lib/auto.py` integration: `--recipe` flag + `init_ledger` from recipe**

  **Goal:** Add `--recipe <name>` flag parsing; route it through `lib/recipes.py::load_and_validate`; pass the recipe's units + phase_order + terminal_phase to `init_ledger`. Without `--recipe`, the picker fires (U8); the picker also routes back through this function.

  **Requirements:** R7, R8, R13, R14.

  **Dependencies:** U3, U1, U7 (the `--recipe` path resolves real recipes + the `A1_BUILTIN` constant, so the recipes — and the constant — must exist before this wiring can be exercised).

  **Files:**
  - Modify: `lib/auto.py` (extend `_parse_args` for `--recipe`, modify `run()` to call recipe loader; bare `/auto` resolves `a1`, falling back to `recipes.A1_BUILTIN` if no `recipes/a1.json` resolves at any tier per KTD-1)
  - Modify: `lib/auto.sh` (no change needed; $ARGUMENTS pass-through already handles new flags)
  - Test: `tests/integration/recipe-r13-regression.test.sh` (new, owned by U4) — asserts `--recipe a1` produces a first-tick ledger byte-identical to v0.1.1's `/auto <plan>` (modulo additive ledger fields). This is the recipe-pipeline-wired-correctly smoke test; it lives in U4 because U4 is the unit that wires the recipe pipeline into init_ledger.

  **Approach:**
  - `_parse_args` (`lib/auto.py:75-114`) extends with `--recipe <value>` clause mirroring the `--adapter` clause (lines 90-95).
  - `run()` at `lib/auto.py:194` becomes:
    ```python
    recipe = recipes.load_and_validate(args["recipe"] or "a1", repo_root)
    ledger.init_ledger(
        repo_root, run_id,
        adapter=adapter,
        units=[recipes.unit_for(u) for u in recipe["units"]],
        loop_phase=recipe["phase_order"][0],
        recipe={"name": recipe["name"], "source_tier": recipe["_source_tier"]},
        phase_order=recipe["phase_order"],
        terminal_phase=recipe["terminal_phase"],
    )
    ```
  - **A1 fallback (KTD-1):** when the resolved name is `a1` and no `recipes/a1.json` resolves at any tier, `recipes.load_and_validate` returns the `recipes.A1_BUILTIN` Python constant (from U7) — so built-in install corruption can't break bare `/auto`. This is NOT "A1 always from JSON": the constant is the canonical source; the JSON is the user-facing override target.
  - **Adapter contract re-lock (KTD-4):** the V1 adapter contract gains the new op `enumerate_plan_units` — but the IMPLEMENTATION lands in U5b (both `ce` and `native` adapters) and the contract doc update lands in U11. U4 does not touch the adapters; it only wires `--recipe` → `init_ledger`. (U4's earlier "adapter contract unchanged" framing was pre-round-2; the contract DOES change in V1.)
  - **v0.1.x integration tests do NOT need updating (repo-research):** `tests/integration/auto-chain.test.sh` and `tests/integration/hooks.test.sh` call `ledger.init_ledger` DIRECTLY, bypassing `lib/auto.py`. So KTD-1's change to the hardcoded path in `run()` — replacing `units=[], loop_phase="plan"` with the recipe pipeline — does NOT alter what those tests exercise; they construct ledgers directly and stay green untouched.
  - `--adapter` flag continues to work; if recipe declares `default_adapter`, the flag overrides.

  **Execution note:** Test-first. Write the regression test "first-tick ledger from `/auto <plan> --recipe a1` ≡ first-tick ledger from v0.1.1 `/auto <plan>`" BEFORE wiring the recipe call.

  **Patterns to follow:**
  - `--adapter` flag parsing at `lib/auto.py:90-95`.
  - `commands/auto.md`'s NL routing prose for how the picker (U8) hands off to this layer.

  **Test scenarios:**
  - Covers R13: `/auto <plan> --recipe a1` produces a first-tick ledger byte-identical to v0.1.1's `/auto <plan>` (modulo the new `recipe`/`phase_order`/`terminal_phase` fields, which are no-ops for A1).
  - Happy path: `/auto <plan> --recipe a2` produces a ledger with 3 plan-phase units + 1 judge work-phase unit.
  - Happy path: `--adapter native --recipe a4` honors both flags.
  - Error path: `--recipe nonexistent` → exit 1 with the resolver's search paths in stderr.
  - Edge case: `--recipe a1 --adapter native` overrides A1's `default_adapter: ce`.

  **Verification:**
  - Existing v0.1.x tests pass unchanged (including `auto-chain.test.sh` / `hooks.test.sh`, which call `init_ledger` directly and are unaffected by the `run()` path change).
  - The R13 regression test passes.
  - `--recipe a2/a4` produces first-tick ledgers matching their topology declarations (a3 deferred to v0.2.1).
  - Bare `/auto` with no `recipes/a1.json` present falls back to `A1_BUILTIN` (KTD-1).

---

- U5. **Engine extension: centralized phase-grammar helper + `_bootstrap.py` shim + AST lint (R12; G2 structurally avoided in V1)**

  **Goal:** Create `lib/phase-grammar.py` (hyphen per repo convention) with `current_phase`/`phase_order`/`terminal_phase`/`next_phase_after_met`/`is_terminal_phase` helpers. **ADD a generic `load_lib_module(name)` to `lib/_bootstrap.py`** (per feasibility F1 — this shim does NOT exist today; `_bootstrap.py` currently only has `load_ledger()`, and hyphenated modules are loaded inline in `lib/tick.py::resolve_adapter`). Rewire every site that currently branches on the `loop_phase` literal to read through the helper. Add an AST lint test that enforces single source of truth mechanically by forbidding the STRING LITERAL `"loop_phase"` anywhere outside `lib/phase-grammar.py` (KTD-3). **The `met` predicate stays SINGLE-field, phase-aware** (per KTD-8) — the two-field `phase_met` carving is DEFERRED to v0.2.1 with A3, so this unit does NOT create a `phase_met` field.

  **Requirements:** R12 (field reservation only — non-default phase ordering is not runnable in V1 per KTD-14), R13.

  **Dependencies:** U1 (ledger fields), U4 (`init_ledger` sets them).

  **Files:**
  - Create: `lib/phase-grammar.py` (loaded via the `lib/_bootstrap.py::load_lib_module(name)` shim that THIS unit adds — `_bootstrap.py` has no such shim today; hyphenated modules are currently loaded inline in `lib/tick.py::resolve_adapter`)
  - Modify: `lib/_bootstrap.py` (**ADD** a generic `load_lib_module(name)` for hyphenated module names — per feasibility F1, this does NOT exist today; reusable for `topology-render.py` too)
  - Modify: `lib/tick.py` (lines 508, 533-562, 610, 634-671 — `_tick_body`, `_maybe_seam`, predicate-met preempts; the `_maybe_seam` plan→seam→work hardcoding becomes recipe-driven via the helper)
  - Modify: `lib/ledger.py` (lines 315-352 — `recompute_predicate` stays SINGLE-field: `met`, now phase-aware via the phase-grammar helper. NO `phase_met` field — the two-field carving is DEFERRED to v0.2.1 with A3 per KTD-8/KTD-14)
  - Modify: `lib/auto-status.py` (status rendering reads phase_order, current_phase, terminal_phase)
  - Test: `tests/unit/phase-grammar.test.sh`
  - Test: `tests/unit/phase-grammar-ast-lint.test.sh` (mechanical enforcement per KTD-3: AST-parses every `lib/*.py`, asserts the STRING LITERAL `"loop_phase"` appears ONLY in `lib/phase-grammar.py` — banning the string literal, not just `ledger["loop_phase"]` subscript shape, catches the bypass class `_key = "loop_phase"; ledger[_key]`, dict iteration, `getattr`, `__getitem__`)

  **Approach:**
  - `recompute_predicate` keeps a SINGLE phase-aware `met` field (per KTD-8 — the two-field `phase_met` carving is DEFERRED to v0.2.1 with A3, which was the only recipe that needed it):
    ```python
    # met: phase-aware via the phase-grammar helper.
    # In loop_phase == "plan": met requires existing plan-rules
    #   (gaps_open==0 AND plan_step=="review_plan"); for A2's N>1 plan-loops,
    #   the per-unit aggregation path from U6 applies (excluding stalled units).
    # In loop_phase == terminal_phase: met requires existing work-rules
    #   (scale-aware findings + all_units_terminal).
    met = compute_met(ledger, phase_grammar.current_phase(ledger))
    ```
    For all V1 recipes (A1/A2/A4) `phase_order` is the default `["plan","seam","work"]` and `terminal_phase` is `"work"`, so `met` is byte-identical to v0.1.1 for A1 and uses the per-unit aggregation path for A2. For backward-compat with v0.1.x ledgers (no `phase_order`/`terminal_phase`): the helper defaults make `met` compute exactly as v0.1.1.
  - `_tick_body`'s switch becomes:
    ```python
    phase = phase_grammar.current_phase(ledger)
    handler = PHASE_HANDLERS.get(phase, handle_work)  # default to work-loop semantics
    handler(...)
    ```
    PHASE_HANDLERS maps phase identity → handler function. `"plan"` → `advance_plan_loop`; `"work"` → `advance_work_loop`. The map is written so it doesn't HARDCODE that plan is first (so v0.2.1's A3 needs no engine change), but in V1 only the default `["plan","seam","work"]` grammar is runnable.
  - `_maybe_seam` becomes a polymorphic `_advance_phase_if_met(ledger)`: reads the single phase-aware `met` field. If `met` and current is not terminal, calls `ledger.transition_and_emit` with the emitter resolved for `(current_phase, next_phase_after_met())` (looked up via the dispatch table + the recipe's per-phase-transition `emitter` hint — U5b), which atomically emits + sets `loop_phase = next_phase_after_met()`. Atomic write per HLTD-Atomic-ordering / `transition_and_emit` (KTD-6).
  - **Two deliberate-fail tests for U5** (A3-specific predicate-carving deliberate-fail removed with A3):
    1. Edit `next_phase_after_met` to always return `"work"` → the AE4 integration test (A2 plan→work transition, owned by U5b) goes RED (proves the helper is the sole decision point for next-phase).
    2. Edit a `lib/*.py` file other than `phase-grammar.py` to add a `"loop_phase"` string literal → AST lint test goes RED (proves mechanical string-literal-class enforcement of KTD-3).

  **Execution note:** Test-first. Write phase-grammar.test.sh and phase-grammar-ast-lint.test.sh BEFORE modifying lib code. (recipe-phase-ordering.test.sh / AE5 is DEFERRED to v0.2.1 with A3.)

  **Patterns to follow:**
  - The `gating_severities()` central helper pattern from `lib/ledger.py` that closed the round-2 narrowing class in v0.1.1 (see `feedback_plan_documents_transition_code_doesnt_wire_it`'s "structural fix" advice).
  - The inline hyphenated-module import in `lib/tick.py::resolve_adapter` (the current ad-hoc way `adapter-ce.py`/`adapter-native.py` are loaded) — generalize that into the new `_bootstrap.py::load_lib_module(name)` shim rather than duplicating it again for `phase-grammar.py`.

  **Test scenarios:**
  - Happy path: A1 ledger → `current_phase` returns `"plan"` at start; after plan-met, `next_phase_after_met` returns `"seam"`.
  - Happy path: A2 ledger → after the parallel plan-loops resolve and the judge transitions, the plan→work advance routes through `next_phase_after_met` (the helper's non-trivial path that A2 exercises in V1).
  - Edge case: A ledger missing `phase_order` (recipe-blind / v0.1.x) → defaults to `["plan", "seam", "work"]`.
  - Edge case: `is_terminal_phase("work")` for a default-grammar ledger → True; `is_terminal_phase("plan")` → False.
  - Resume test: a paused A2 run mid-`plan` phase has `loop_phase == "plan"`. `current_phase(ledger)` returns `"plan"` (the saved value), NOT `phase_order[0]` directly. This is the HLTD start-phase-propagation invariant (load-bearing for resume).
  - Reserved-field test (KTD-14): a recipe declaring non-default `phase_order` (e.g. A3's `["work_sketch","review","plan","work_refine"]`) PARSES but the validator REJECTS the non-default value — no non-default phase grammar is runnable in V1.
  - Backward-compat: a v0.1.x ledger (no `phase_order`) → single phase-aware `met` computed identically to v0.1.1.
  - `_bootstrap.py` shim: `load_lib_module("phase-grammar")` returns the loaded module (the shim THIS unit adds); `load_lib_module("topology-render")` also works (reusability).
  - Integration: A1 still runs unchanged — first unit is plan, then work, then exit on P3-only.
  - AST lint: every `lib/*.py` file other than `phase-grammar.py` contains zero `"loop_phase"` string literals (the string-literal class, per KTD-3 — not just `ledger["loop_phase"]` subscript shape).
  - Deliberate-fail #1 (Edit-tool revert of `next_phase_after_met` to always return `"work"`): the AE4 integration test (A2 plan→work, owned by U5b) goes RED.
  - Deliberate-fail #2 (Edit-tool add a `"loop_phase"` string literal in another module): AST lint RED.

  **Verification:**
  - All v0.1.x tests pass unchanged.
  - Both deliberate-fail checks confirmed RED then restored.
  - AST lint test: zero `"loop_phase"` string literals outside `lib/phase-grammar.py`.
  - `lib/_bootstrap.py::load_lib_module` exists and loads hyphenated modules.

---

- U5b. **Engine extension: phase-transition emitter registry (R12, G1)**

  **Goal:** Create `lib/emitters.py` with the named-function registry (KTD-5, resolves G1) — **exactly 3 emitters in V1** (A3's `review_findings_to_plan_input` deferred to v0.2.1 per KTD-14). Add the new ledger primitive `ledger.transition_and_emit(emitter_fn, ...)` (KTD-6) that does transition + emit + recompute + atomic-write in ONE `_with_locked_ledger` body. Wire the seam-handler (`_advance_phase_if_met` from U5) to look up the emitter (via the `(from_phase, to_phase)` tuple plus the recipe's per-phase-transition `emitter` hint that disambiguates A1 vs A4 at the shared `(plan, work)` boundary) and call `transition_and_emit`, which atomically appends new pending units to `units[]` before predicate recomputation (HLTD-Atomic-ordering, resolves G3). Add the new adapter op `enumerate_plan_units(ledger) → list[unit_dict]` to BOTH adapters — it is the producer the emitters read (KTD-4, resolves the producer gap).

  **Requirements:** R11, R12, R13 (A1's emitter must produce v0.1.x-equivalent work units).

  **Dependencies:** U1, U5 (phase-grammar helper), U7 (the recipes inform which emitters are needed and carry the `emitter` hint).

  **Files:**
  - Create: `lib/emitters.py` (V1 emitters — exactly 3: `plan_output_to_work_units`, `judge_winner_to_work_units`, `plan_output_to_paired_builders`; plus a NAME→function registry. The recipe's `phase_transitions` block names which emitter fires per boundary — so no tuple-collision guessing (A1 and A4 each name their own emitter at the shared `(plan, work)` boundary). A3's `review_findings_to_plan_input` is DEFERRED to v0.2.1 per KTD-14.)
  - Modify: `lib/ledger.py` (add the new `transition_and_emit(emitter_fn, ...)` primitive per KTD-6 — a single `_with_locked_ledger` body: read ledger → apply transition → call emitter → append returned units → recompute predicate → atomic write)
  - Modify: `lib/adapter-ce.py` (implement the new `enumerate_plan_units(ledger) → list[unit_dict]` op — the V1 adapter-contract re-lock per KTD-4)
  - Modify: `lib/adapter-native.py` (implement `enumerate_plan_units` — both adapters must)
  - Modify: `lib/tick.py` (the seam-handler from U5 calls `ledger.transition_and_emit` with the looked-up emitter)
  - Test: `tests/unit/emitters.test.sh`
  - Test: `tests/integration/recipe-parallel-plans.test.sh` (AE4: A2 with 3 plan-loops; ledger shape verification + judge gating; owned by U5b per AE distribution)
  - Test: `tests/integration/recipe-adversarial-pair.test.sh` (NEW A4 runtime end-to-end test — resolves adversarial F2: A4 needs its OWN integration test, not A1's emitter reused; asserts `plan_output_to_paired_builders` fires at A4's `(plan, work)` boundary and emits 2 biased builders + 1 comparator; owned by U5b, replaces the deferred AE5 in the AE distribution)

  **Approach:**
  - **Emitters are PURE functions** (signature `emit(ledger, triggering_unit) → list[new_unit_dict]`): input current ledger state + the unit whose transition triggered the emission; output new units to append. **They MUST NOT call `record_verdict` / `transition` / `set_loop` or any other ledger mutator** — the seam-handler already owns the lock when it calls the emitter, and `_flock_run` opens a NEW fd on each call, so a mutator call from inside the emitter re-enters the lock and DEADLOCKS (KTD-6). The emitter only computes the new-unit list; the primitive does all writing.
  - **The producer (resolves the producer gap, KTD-4):** the emitters do NOT read plan output from a ledger field — v0.1.1 never produced work units in code. They read it via the NEW adapter op `enumerate_plan_units(ledger) → list[unit_dict]`, which both `ce` and `native` adapters implement (the V1 adapter-contract re-lock). The plan-loop, on reaching `plan-done`, makes this op's output available; the emitter reads it.
  - `plan_output_to_work_units(ledger, plan_unit)` (A1, + A2's chosen-plan downstream): reads the plan output via `enumerate_plan_units`, emits one work-phase unit per item. This is what v0.1.1's `_maybe_seam` does today, generalized.
  - `judge_winner_to_work_units(ledger, judge_unit)` (A2): reads judge_unit's `findings` array for an entry shaped `{"winner_unit_id": "plan-1"}` (or similar — exact shape locked in U5b's contract), finds that plan unit in the ledger, emits that plan's `enumerate_plan_units` output as work units.
  - `plan_output_to_paired_builders(ledger, plan_unit)` (A4): reads plan output via `enumerate_plan_units`; emits two work-phase units with `dispatch_context.bias` set to `"clarity"` and `"perf"` respectively + a third comparator work-phase unit with `depends_on: [both]`.
  - (A3's `review_findings_to_plan_input` is DEFERRED to v0.2.1 per KTD-14 — not implemented in V1.)
  - **Emitter disambiguation (resolves adversarial F2):** A1 and A4 share the `(plan, work)` tuple, so the dispatch table alone can't pick. The recipe carries a per-phase-transition `emitter` hint (per HLTD); `transition_and_emit`'s lookup reads that hint to choose between `plan_output_to_work_units` (A1) and `plan_output_to_paired_builders` (A4). If no emitter resolves for a boundary, the engine raises.
  - **Emitter keyed on the ARRIVING phase (`to`), settled in U7's renderer build 2026-05-25):** a `phase_transitions` entry `{from, to, emitter}` fires when the run ARRIVES at `to` — the emitter produces units FOR the `to` phase. A1 declares `{from: plan, to: work}` but phase_order routes `plan → seam → work`; the emitter fires arriving at `work` (the seam is a pass-through). So U5b's seam-handler, when advancing INTO phase X, runs the emitter whose `to == X` (NOT the emitter for the exact adjacent `(prev, X)` pair). This matches `lib/topology-render.py::_emitter_for_arrival`, which U7 already implements and tests.
  - **Atomic ordering via the new primitive** (HLTD-Atomic + G3 resolution, KTD-6): the seam-handler does NOT call `record_verdict` then a separate emit-write (the round-1 framing's torn-state bug). Instead it calls the new `ledger.transition_and_emit(emitter_fn, ...)` primitive, which inside ONE `_with_locked_ledger` body does: (1) apply the triggering transition, (2) call the (pure) emitter, (3) append returned units to `units[]`, (4) recompute predicate on the post-emission unit set, (5) atomic write. The primitive is what makes the snapshot atomic — without it, steps would span separate locked writes with a crash window between transition and emission.

  **Execution note:** Test-first. AE4 integration test BEFORE writing emitters. Each emitter gets a unit test with happy-path + at-least-one-edge-case before its implementation.

  **Patterns to follow:**
  - `lib/orchestrator.py::_normalize_unit` for the new unit shape (each emitter's output must conform).
  - `lib/ledger.py::_with_locked_ledger` for the atomic-write discipline.

  **Test scenarios:**
  - Happy path (`plan_output_to_work_units`): plan unit with 3 top-level plan items → 3 work-phase units with id derived from plan item title, `depends_on: []`, phase `"work"` (or recipe's chosen work-phase name).
  - Edge case: plan unit with empty plan output → emitter returns `[]` (no units emitted); the engine then... what? Surface as a test: "A1 with empty plan output → predicate fires `met=true` immediately with zero work units? Or hangs? The vacuous-exit guard in `recompute_predicate` should fire."
  - Happy path (`judge_winner_to_work_units`): judge unit findings list `[{"winner_unit_id": "plan-2"}]` → emit plan-2's work units (via `enumerate_plan_units`).
  - Edge case: judge findings don't name a winner → emitter raises with clear error (engine treats as judge failure).
  - Happy path (`plan_output_to_paired_builders`): plan output emits 2 work units with biases (`clarity`/`perf`) + 1 comparator with `depends_on: [builder-a, builder-b]`.
  - Disambiguation (resolves adversarial F2): at the `(plan, work)` boundary, an A1 recipe's `emitter` hint resolves `plan_output_to_work_units` and an A4 recipe's `emitter` hint resolves `plan_output_to_paired_builders` — same tuple, different emitter, picked by the hint.
  - Re-entrancy guard: an emitter that (incorrectly) calls a ledger mutator is caught — `transition_and_emit` documents the pure-function contract; a test asserting an emitter calling `record_verdict` deadlocks/errors confirms the constraint is load-bearing, not convention.
  - **Atomic ordering deliberate-fail**: Edit `transition_and_emit` to recompute predicate BEFORE running the emitter on an A2 ledger fixture (judge terminal, no work units yet). Assert predicate fires `met=true` (incorrect). AE4 integration test goes RED. Restore.
  - Covers AE4: A2 run, 3 plan units dispatched after first tick; after all return verdicts, judge dispatches; before judge terminal, no work units; after judge terminal with winner findings, work units appear in `units[]`; run does NOT exit until those work units are terminal.
  - Covers the new A4 runtime integration test (`recipe-adversarial-pair.test.sh`, resolves adversarial F2): an A4 run, after plan resolves, has `plan_output_to_paired_builders` fire — 2 biased builder units + 1 comparator (`depends_on` both) appear in `units[]`; run does NOT exit until the comparator is terminal. This is A4's OWN end-to-end test, not A1's emitter reused.

  **Verification:**
  - All v0.1.x tests pass (A1's `plan_output_to_work_units` produces v0.1.1-equivalent work units — the R13 regression test verifies this).
  - AE4 integration test passes; the new A4 `recipe-adversarial-pair.test.sh` integration test passes; atomic-ordering deliberate-fail confirmed RED then restored.
  - Each of the 3 V1 emitters has a passing unit test with at least one edge case.
  - `enumerate_plan_units` is implemented + tested on both `ce` and `native` adapters; `ledger.transition_and_emit` exists and does transition+emit+recompute+atomic-write in one locked body.

---

- U6. **Engine extension: per-unit `plan_step`/`gaps_open` + serialized N-parallel plan-loop advance (R11, KTD-4)**

  **Goal:** When a recipe declares N>1 plan-phase units, the engine advances them **one at a time per tick** (serialized round-robin), with per-unit `plan_step`/`gaps_open` fields driving the predicate. A1's single plan-loop continues to use top-level scalars (byte-identical to v0.1.1). The `next_plan_step(ledger)` adapter op SIGNATURE is preserved unchanged because only one plan unit is mid-advance at any moment. (Note: the v0.2.0 adapter contract DOES re-lock — it adds the new op `enumerate_plan_units` per KTD-4 — but that op is consumed by the emitters in U5b, not by this serialized-advance path.)

  **Requirements:** R11, R13, KTD-4.

  **Dependencies:** U1, U5 (the phase-grammar helper).

  **Files:**
  - Modify: `lib/tick.py::advance_plan_loop` (lines 376-441) — when N>1 plan units, pick ONE ready plan unit per tick (round-robin)
  - Modify: `lib/ledger.py::recompute_predicate`'s plan-phase branch (single phase-aware `met` field per KTD-8 — the two-field `phase_met` carving is DEFERRED to v0.2.1 with A3) — reads per-unit gaps/plan_step when present
  - Modify: `lib/ledger.py::set_loop` — accept per-unit plan_step + `last_advanced_at` writes
  - Modify: `lib/orchestrator.py` — add round-robin selection helper `pick_next_plan_unit_to_advance(ledger)` that returns one ready plan unit per call, rotating across ticks via the per-unit `last_advanced_at` field
  - Test: `tests/unit/orchestrator.test.sh` (extend with round-robin selection + tiebreaker + stalled-exclusion tests)

  **Approach:**
  - `advance_plan_loop` becomes:
    ```python
    plan_units = [u for u in ledger["units"] if u.get("phase", "plan") == "plan" and u["state"] == "dispatched"]
    if len(plan_units) <= 1:
        # Fast path — scalar plan_step/gaps_open, unchanged behavior for A1
        # (preserves R13 byte-identical regression)
        ...
    else:
        # Serialized N>1 path — pick one to advance this tick
        active_unit = orchestrator.pick_next_plan_unit_to_advance(ledger)
        adapter.next_plan_step(ledger)  # next_plan_step signature UNCHANGED per KTD-4
        # write top-level plan_step (this tick's value) AND per-unit plan_step + last_advanced_at on active_unit
        set_loop(plan_step=new_step,
                 per_unit_plan_step={active_unit["id"]: new_step},
                 last_advanced_at={active_unit["id"]: now()})
    ```
  - **`next_plan_step` signature stays unchanged** (KTD-4). The top-level `plan_step` reflects whichever unit is actively advancing this tick. Other plan units are dispatched-but-idle: their `state` is `dispatched`, their per-unit `plan_step` is whatever it was last time they were the active unit. The adapter reads `ledger["plan_step"]` as before; it sees one logical advance-stream per tick. (The contract's NEW op `enumerate_plan_units` is the producer the emitters read in U5b — it does not participate in this advance path.)
  - **Round-robin tiebreaker (resolves feasibility F5 / adversarial F9):** `pick_next_plan_unit_to_advance(ledger)` returns the ready plan unit (state `dispatched`, phase `plan`) with the OLDEST per-unit `last_advanced_at` (the field U1 lists). A `null` `last_advanced_at` sorts as oldest (never-advanced units go first); remaining ties break by `units[]` declaration order. Each advance stamps the active unit's `last_advanced_at` with the current time, so the unit just advanced sorts youngest and the rotation moves to the next-oldest unit next tick.
  - **Stalled plan unit recovery (resolves adversarial F3):** if a plan unit goes `stalled`, three things follow, ALL of which must hold so a stall surfaces rather than silently hangs:
    1. The round-robin ready-set EXCLUDES it (`pick_next_plan_unit_to_advance` only considers `state == "dispatched"` plan units), so the rotation continues across the remaining live plan units.
    2. The predicate's plan-phase aggregation EXCLUDES stalled units from the "all plan units satisfy" universe (analogous to `terminal-skip`) — a stalled unit does NOT block plan-met for the units that can still complete.
    3. The judge's (or any work unit's) `depends_on` gate treats a stalled dependency as a HARD BLOCK, surfaced to the operator via `auto-status` — NOT a silent hang. `auto-status` must report "judge blocked on stalled plan unit <id>" so the operator can intervene (re-dispatch or skip).
  - `recompute_predicate`'s plan-phase branch (single phase-aware `met` field per KTD-8; the two-field `phase_met` carving is DEFERRED to v0.2.1 with A3):
    - If ledger has top-level `gaps_open != None` AND no per-unit gaps_open fields populated, use top-level (A1 fast path).
    - Else aggregate over plan-phase units' per-unit `gaps_open`, EXCLUDING stalled units (per the stalled-recovery path above): plan-`met` requires all non-stalled plan units have per-unit `plan_step == "review_plan" AND gaps_open == 0`.
  - **Resume note (HLTD start-phase-propagation invariant):** if a run is paused mid-N-plan-loop advance, the ledger carries each plan unit's `plan_step`, `gaps_open`, and `last_advanced_at` (per-unit). On resume, `pick_next_plan_unit_to_advance` picks up where it left off — round-robin state lives in the ledger's per-unit field values, not in memory.

  **Execution note:** Test-first. Write the AE4 integration test (A2 with 3 plan-loops; ledger after first tick has 3 dispatched plan units; judge waits) BEFORE modifying advance_plan_loop.

  **Patterns to follow:**
  - Existing fan-out in `lib/orchestrator.py::ready_units` and `dispatch_batch` — the orchestrator is already topology-agnostic; reuse it.
  - The fast-path-when-scalar pattern from `lib/ledger.py`'s existing adapter_scale handling.

  **Test scenarios:**
  - Covers AE4: A2 run with 3 plan-phase units. After first tick: ledger shows 3 units in `dispatched` (plan-phase). After all 3 return verdicts: judge unit dispatches. Before judge is terminal: no work units dispatch.
  - Covers R13: A1 first-tick ledger byte-identical to v0.1.1 (one plan unit means scalar plan_step is used, per-unit fields absent).
  - Happy path: per-unit `gaps_open` aggregates correctly across non-stalled units (all must be 0 for plan-met).
  - Round-robin tiebreaker: 3 ready plan units with mixed `last_advanced_at` (one null, two timestamped) → `pick_next_plan_unit_to_advance` returns the null one first; after it advances and is stamped, the next call returns the oldest-timestamped of the remaining; ties (e.g. all null at start) break by `units[]` declaration order.
  - Round-robin resume: ledger paused mid-rotation with per-unit `last_advanced_at` populated → on resume, `pick_next_plan_unit_to_advance` continues the rotation correctly (oldest first), not from the top.
  - Edge case (stalled — resolves adversarial F3): one of A2's plan units stalls → it is EXCLUDED from the round-robin ready-set; the remaining live plan units keep advancing; the predicate's plan-phase aggregation excludes the stalled unit so plan-met can still be reached by the live units; the judge's `depends_on` gate treats the stalled dependency as a HARD BLOCK surfaced via `auto-status` ("judge blocked on stalled plan unit <id>"), NOT a silent hang.
  - Edge case: one of A2's plan units returns `terminal-skip` → judge can dispatch with remaining units.
  - Deliberate-fail: revert `advance_plan_loop` to single-unit-only path → AE4 test RED. Restore.

  **Verification:**
  - All v0.1.x tests pass.
  - AE4 integration passes; deliberate-fail check.
  - `recompute_predicate`'s plan branch handles both scalar (A1) and per-unit (A2) shapes.

---

- U7. **Built-in recipes: a1.json, a2.json, a4.json, w.json (A3 deferred to v0.2.1)**

  **Goal:** Write the four canonical built-in recipe JSON files for v0.2.0 (A1, A2, A4, W work-only). (A3's `a3.json` is DEFERRED to v0.2.1 per KTD-14 — it needs the full non-default `phase_order` machinery the V1 validator rejects; W's `["work"]` is the one carve-out per KTD-15.) The canonical A1 topology is ALSO a Python constant `A1_BUILTIN` in `lib/recipes.py` per KTD-1.

  **Requirements:** R2, KTD-10, KTD-15.

  **Dependencies:** U2 (validator + schema), U5b (the emitter table for A2/A4 dynamic-emission AND the `enumerate_plan_units` op that W reuses at init).

  **Files:**
  - Create: `recipes/a1.json` (the user-facing override target; `lib/recipes.py::A1_BUILTIN` is the constant fallback per KTD-1)
  - Create: `recipes/a2.json`
  - Create: `recipes/a4.json`
  - Create: `recipes/w.json` (work-only — `phase_order: ["work"]`, `terminal_phase: "work"`, no `phase_transitions`, no plan unit; units come from `enumerate_plan_units` at init per KTD-15)
  - Modify: `lib/recipes.py` (add the `A1_BUILTIN` constant per KTD-1)
  - Modify: `lib/auto.py` / `lib/ledger.py` init path (the W init-time `enumerate_plan_units` call — when the resolved recipe's `phase_order` is `["work"]` and it has no declared units, call the adapter's `enumerate_plan_units` once and load the result as initial `pending` work units. This is the ONE place W differs from A1/A2/A4 at init; coordinate with U4's init wiring.)
  - Create: `lib/topology-render.py` (single ASCII renderer per KTD-10; called by picker U8 + skill U9 + auto-status). Signature: `render(recipe_dict, width_hint) → str`. The four built-in cards (A1/A2/A4 + a W card) are the conformance fixtures.
  - Test: `tests/unit/recipes.test.sh` (conformance per file + `A1_BUILTIN` validates and equals resolved `recipes/a1.json`)
  - Test: `tests/integration/recipe-work-only.test.sh` (NEW — W loads units from `enumerate_plan_units` at init, starts in `work` phase, no plan-loop runs; owned by U7/U5b)
  - Test: `tests/unit/topology-render.test.sh` (renders each built-in + an edge case for a user-shaped recipe)

  **Approach:**
  - A1: see HLTD. Single plan unit (id `plan`, phase `plan`, invokes `next_plan_step`). Default `phase_order: ["plan", "seam", "work"]` and `terminal_phase: "work"`. **The same topology is also the `A1_BUILTIN` Python constant in `lib/recipes.py` (KTD-1)** — `recipes/a1.json` is committed so the override path and conformance corpus exist, but the constant is the runtime fallback so built-in install corruption can't break bare `/auto`.
  - W (work-only, KTD-15): `{"name": "w", "phase_order": ["work"], "terminal_phase": "work", "units": []}` — no plan unit, no `phase_transitions`. At `init_ledger`, because `phase_order == ["work"]` and `units` is empty, the engine calls `adapter.enumerate_plan_units(ledger)` on the operator's provided plan/spec ONCE and loads the result as initial `pending` work-phase units. The run starts directly in the work-loop. This is the "I already have a reviewed plan, just build it" path (field feedback 2026-05-25). No emitter (units are loaded, not emitted); the existing work-loop predicate applies unchanged since `work` is the terminal phase.
  - A2: see HLTD. 3 plan units (`plan-1`, `plan-2`, `plan-3` — all phase `plan`, all invoke `next_plan_step`) + 1 judge work-phase unit (id `judge`, depends_on all 3 plan units, invokes `review` with `prompt_template: "judge.md"`). Work units emit dynamically via `judge_winner_to_work_units` (U5b). The plan→work phase-transition carries an explicit `emitter: "judge_winner_to_work_units"` hint per HLTD (disambiguates from A1/A4 at the same boundary).
  - A4: see HLTD. Plan unit (phase `plan`, invokes `next_plan_step`). Builder + comparator units emit dynamically via `plan_output_to_paired_builders` (U5b). **A4's `(plan, work)` phase-transition MUST carry an explicit `emitter: "plan_output_to_paired_builders"` hint** (per HLTD disambiguation — A1 and A4 share the `(plan, work)` tuple, so the recipe-side `emitter` hint per phase-transition is what tells the dispatch which emitter fires). A1's recipe carries `emitter: "plan_output_to_work_units"` for the same reason.
  - Each recipe has a `description` field for the picker (one-line summary).
  - **A3 deferral note:** `recipes/a3.json` is NOT created in v0.2.0. Its sketch (non-default `phase_order: ["work_sketch", "review", "plan", "work_refine"]`, `terminal_phase: "work_refine"`) is preserved in the v0.2.1 brainstorm. The V1 validator (U2) rejects non-default `phase_order` values, so shipping a3.json now would fail load and the picker couldn't surface it.

  **Execution note:** None — pure data file authoring plus the `A1_BUILTIN` constant.

  **Patterns to follow:**
  - The A1/A2/A4 ASCII topology cards in `docs/brainstorms/2026-05-23-auto-v0.2.0-recipes-requirements.md` lines 113-158. Each JSON must encode the topology shown in its card. (A3's card is preserved for the v0.2.1 plan.)

  **Test scenarios:**
  - Each of a1/a2/a4 validates against `recipes/schema.json`.
  - `load_and_validate("a1", repo_root)` etc. return the expected topology.
  - A1's units exactly match v0.1.1's hardcoded initial state; `A1_BUILTIN` constant equals the resolved `recipes/a1.json` topology (KTD-1 — no drift between the two sources).
  - Bare resolution with no `recipes/a1.json` at any tier falls back to `A1_BUILTIN` (KTD-1).
  - A2's `units` includes 3 plan-phase units + 1 judge work-phase unit; judge's `depends_on` lists all 3 plan unit IDs.
  - A2 and A4 each carry the correct per-phase-transition `emitter` hint (A2: `judge_winner_to_work_units`; A4: `plan_output_to_paired_builders`; A1: `plan_output_to_work_units`).

  **Verification:**
  - All three files validate.
  - `A1_BUILTIN` validates and matches `recipes/a1.json`.
  - Picker (U8) sees them (3 cards, not 4).
  - Integration tests pass.

---

- U8. **Picker in `commands/auto.md` (NL routing)**

  **Goal:** When `/auto <plan>` is invoked without `--recipe`, the picker fires: it calls `list_available`, presents the recipes via `AskUserQuestion`, and re-invokes the run with the chosen recipe.

  **Requirements:** R6, R7, R8, F1, F3, KTD-10.

  **Dependencies:** U3 (loader/list_available), U4 (--recipe wiring), U7 (the topology-render module).

  **Files:**
  - Modify: `commands/auto.md` (NL routing prose; the existing one-`$ARGUMENTS`-line invariant must hold)
  - Create: `lib/recipes-list.sh` (shell wrapper calling `lib/recipes.py::list_available`, emits parseable form for orchestrator consumption)
  - Test: `tests/smoke/scaffold.test.sh` (no change expected; verify the existing assertion holds)
  - Test: `tests/integration/recipe-picker.test.sh` (AE1 + AE2: picker enumerates 3 built-ins A1/A2/A4; workspace shadows global; owned by U8 per AE distribution)

  **Approach:**
  - In `commands/auto.md`'s NL routing prose, add a new rule: "If `$ARGUMENTS` is empty OR is just a plan path with no `--recipe` flag, run `bash "${CLAUDE_PLUGIN_ROOT}/lib/recipes-list.sh"` to enumerate available recipes (calls `lib/recipes.py::list_available`), then call `AskUserQuestion` with one option per recipe (label = name + tier badge, description = recipe's `description` field, preview = `lib/topology-render.py::render(recipe_dict)` output if the preview field renders multi-line ASCII; otherwise omit preview). On selection, synthesize the final `bash` invocation with `--recipe <chosen-name>` appended and call it directly via the Bash tool (bypassing the canonical dispatch line per the NL routing pattern)."
  - **AskUserQuestion preview surface decision (deferred-to-implementation per KTD-9):** Try the multi-line ASCII `preview` field first using `lib/topology-render.py`. If rendering breaks, fall back to short labels with descriptions; users can run a separate command (TBD during implementation, possibly `/auto --show-topology a2`) to see the visual once.
  - `--recipe` pass-through: if `$ARGUMENTS` includes `--recipe <name>`, NO picker; canonical dispatch line fires.
  - `recipes-list.sh` is a new lib helper that calls `lib/recipes.py::list_available` and emits a parseable form for the orchestrator.
  - **The renderer is `lib/topology-render.py` (from U7), called the same way from the authoring skill (U9) and from `auto-status` topology output** — single source of truth per KTD-10.

  **Execution note:** Smoke-test invariant (`tests/smoke/scaffold.test.sh:87-109`) MUST continue to pass. The picker prose CANNOT add new `$ARGUMENTS` references outside the existing dispatch line.

  **Patterns to follow:**
  - The existing NL routing prose in `commands/auto.md` (the bare-`/auto`/freeform-sentence routing landed in v0.1.1).
  - Memory `feedback_slash_command_nl_routing_pattern` for the orchestrator-routes-then-invokes pattern.

  **Test scenarios:**
  - Covers AE1: `/auto <plan>` with no `--recipe` flag — picker fires with 3 options (a1/a2/a4, all "built-in"; A3 deferred to v0.2.1 per KTD-14). User selecting "a1" produces a run indistinguishable from v0.1.1.
  - Covers AE2 (UX side): with a workspace `team-review.json` saved, picker shows team-review FIRST labeled "workspace", followed by a1/a2/a4.
  - Covers R8: `/auto <plan> --recipe a2` skips picker entirely. (Already tested in U4; this verifies the routing prose lets it through.)
  - Smoke: `tests/smoke/scaffold.test.sh` still asserts exactly one `$ARGUMENTS` line in `commands/auto.md`, and it IS the canonical dispatch line.

  **Verification:**
  - Smoke test passes unchanged.
  - The picker visibly fires on bare `/auto <plan>` (manual smoke).
  - `--recipe <name>` continues to bypass the picker.

---

- U9. **Authoring skill: `auto-author-recipe`**

  **Goal:** A skill that compiles recipes from prose (entry A: "I want a workflow that does X") and from completed-run ledgers (entry B: "save the topology that just ran"). Both produce validated JSON written to the user-chosen tier (workspace or global).

  **Requirements:** R9, R10, F2.

  **Dependencies:** U2 (validator), U3 (registry — for save-target paths), U7 (the topology-render module).

  **Files:**
  - Create: `skills/auto-author-recipe/SKILL.md`
  - Create: `skills/auto-author-recipe/references/visual-vocabulary.md` (the contract doc for `lib/topology-render.py`, NOT a separate renderer)
  - Test: `tests/integration/recipe-authoring.test.sh` (AE3: skill produces A4-shape from prose; owned by U9 per AE distribution)

  **Approach:**
  - Entry A (prose elicitation): Skill asks the user a structured set of questions — what's the goal of the workflow? what runs in parallel vs sequentially? are there feedback loops where one phase informs an earlier one? what severity gates the exit? Skill compiles answers into a recipe draft, renders the topology back as ASCII via `lib/topology-render.py::render(recipe_dict)` (same renderer the picker uses — KTD-10), and asks the user to confirm/revise.
  - Entry B (reverse-derive from a completed run): Skill reads `<repo>/.claude/auto/<run-id>.json` after the run is done, extracts the topology (`units`, `phase_order`, `terminal_phase`), and produces a recipe draft. User reviews and optionally renames.
  - On confirm, skill asks: workspace (this project) or global (personal)? Then writes the file using `lib/recipes.py::validate_and_lint` to gate the write (KTD-2). Hard errors block; lint warnings surface to the user but don't block. After the user resolves any warnings, the skill writes.
  - **Atomic write (per feasibility F7):** the recipe file MUST be written ATOMICALLY — write to a temp file via `mkstemp` in the same target directory, then `os.rename` into place. A partial/interrupted write must never leave a corrupt recipe file that the loader or picker would then trip over. (Same atomic-write discipline as `lib/ledger.py::_atomic_write`.)
  - **Verification step (per memory `feedback_subagent_write_verification`)**: after the atomic rename, skill reads it back, runs `load_and_validate`, confirms the topology renders to the same ASCII it showed before write. Only then reports "saved."

  **Execution note:** None. Skill design.

  **Patterns to follow:**
  - Existing skills under `skills/` (auto/SKILL.md, auto-adapter/SKILL.md) for the SKILL.md structure.
  - Memory `feedback_slash_command_nl_routing_pattern` — skill drives elicitation in prose, calls Bash for the actual file write.

  **Test scenarios:**
  - Covers AE3: Skill given prompt "two builders, same plan, one optimizes for clarity and one for perf, then a comparator picks" produces a recipe whose topology matches A4 (two work-phase units with empty `depends_on` + a comparator with `depends_on` listing both).
  - Happy path (entry B): After an A2 run completes, skill reads the ledger, produces a recipe with 3 plan-phase units + 1 judge unit matching the ran topology.
  - Edge case: Skill asks the user to clarify when the prose is ambiguous ("one builder, two reviewers" — does the second reviewer depend on the first?).
  - Error path: Skill produces a draft that fails validation → skill surfaces the validator's error to the user, doesn't write the file.
  - Verification step: After write, the read-back validates. If validation fails on read-back, skill reports "save failed" and removes the file.

  **Verification:**
  - AE3 conformance test passes.
  - Skill ships as `skills/auto-author-recipe/SKILL.md` and is auto-loaded by the plugin manifest.

---

- U10. **Verification gate: per-phase deliberate-fail audit + test-count check (no new test code — verification only)**

  **Goal:** Confirm every AE has a passing test + a verified deliberate-fail check. Confirm test count growth. This unit produces no new code — it's a checkpoint that gates merge. **Per scope-guardian SG-2, the audit is SPLIT per phase** (Phase 1 audits its own deliberate-fail checks at the end of Phase 1; Phase 2 audits the rest) so each phase is an independently-meaningful landing point with its own verified gate, not a deferred all-at-the-end audit.

  **Requirements:** R2, R5, R7, R9, R10, R11, R12 (field-reservation only — non-default phase ordering is not runnable in V1 per KTD-14), R13.

  **Dependencies:** U1–U9 + U5b (all the integration tests must exist, distributed into owning units per the AE-per-unit distribution decision).

  **Files:**
  - None created. Test artifacts already produced by owning units:
    - `tests/integration/recipe-picker.test.sh` (AE1, AE2) — U8
    - `tests/integration/recipe-authoring.test.sh` (AE3) — U9
    - `tests/integration/recipe-parallel-plans.test.sh` (AE4) — U5b
    - `tests/integration/recipe-adversarial-pair.test.sh` (A4 runtime end-to-end; resolves adversarial F2; replaces deferred AE5) — U5b
    - (AE5 / `recipe-phase-ordering.test.sh` is DEFERRED to v0.2.1 with A3 — not in v0.2.0's audit set.)

  **Approach:**
  - **AE distribution decision** (resolves pattern-recognition #5): each AE test lives with its owning unit, not consolidated in a single `recipes-e2e.test.sh`. Follows the repo convention of one integration test file per surface. v0.2.0's AE set is AE1+AE2 (U8), AE3 (U9), AE4 + the new A4 runtime test (U5b). AE5 is deferred with A3.
  - **Phase 1 audit pass** (runs at the end of Phase 1, before Phase 2 starts — SG-2): audit the deliberate-fail checks owned by Phase 1 units (U1, U2, U3, U5, U5b, U6) + confirm AE4 + the A4 runtime test + R13 regression are green. This makes Phase 1 a genuinely-audited landing point on its own.
  - **Phase 2 audit pass** (runs at the end of Phase 2): audit the deliberate-fail checks owned by Phase 2 units (U9) + confirm AE1/AE2/AE3 green.
  - For each deliberate-fail check, in BOTH passes, manually:
    1. Apply the deliberate-fail edit via the Edit tool (NOT a script — per memory `feedback_deliberate_fail_revert_via_edit_not_inscript`)
    2. Run the test
    3. Confirm RED
    4. Restore via Edit
    5. Confirm GREEN
  - Document the verified count in a per-PR checklist (one checklist per phase PR).

  **Execution note:** This is a quality gate, not an implementation unit. Each phase's audit cannot be parallelized within its phase (must run after that phase's other units).

  **Test scenarios:**
  - Test expectation: none — this unit is a verification gate, not a feature-bearing unit.

  **Verification:**
  - `tests/run.sh all` reports ≥ 210 tests (current 184 + ~30 new across unit + integration units U1–U9 + U5b; lower than the pre-round-2 ≥220 estimate because A3's tests — AE5, the two-field `phase_met` predicate-carving tests, the `review_findings_to_plan_input` emitter test, and the A3-specific phase-grammar scenarios — are removed per KTD-14, partially offset by the new A4 runtime test + round-robin tiebreaker/stalled-exclusion tests).
  - All v0.2.0 AE tests pass (AE1, AE2, AE3, AE4 + the A4 runtime test).
  - **Phase 1:** every deliberate-fail check from U1, U2, U3, U5, U5b, U6 verified RED then restored; checklist in the Phase 1 PR.
  - **Phase 2:** every deliberate-fail check from U9 verified RED then restored; checklist in the Phase 2 PR.

---

- U11. **Docs + plugin manifest + version bump**

  **Goal:** Update `README.md`, `docs/contracts/ledger-schema.md`, `docs/contracts/recipe-format.md`, `.claude-plugin/plugin.json` (version 0.1.1 → 0.2.0). The marketplace re-sync is a follow-up.

  **Requirements:** Doc impact; version bump is a release-management requirement.

  **Dependencies:** U1–U10 (so the docs describe what shipped).

  **Files:**
  - Modify: `README.md` (recipe section, picker UX, authoring skill, the three built-ins A1/A2/A4 — A3 deferred to v0.2.1 per KTD-14)
  - Modify: `docs/contracts/ledger-schema.md` (additive sections marked LOCKED): new top-level fields (`recipe`, `phase_order`, `terminal_phase`), new per-unit fields (`phase`, `plan_step`, `gaps_open`, `dispatch_context`, `last_advanced_at`), new I-1 corollary: "phase-transition emission writes new pending units in the same atomic snapshot as the verdict-observation that triggers it; predicate recomputation runs on the post-emission unit set" (G3). (No `phase_met` field — the two-field predicate carving is DEFERRED to v0.2.1 with A3 per KTD-8/KTD-14; `met` stays single-field, phase-aware. The emitter signature + atomic-ordering rule live as module docstrings in `lib/emitters.py` plus this I-1 corollary, NOT a separate contract doc — per scope-guardian SG-1.)
  - Modify: `docs/contracts/adapter-contract.md` (**the V1 adapter contract RE-LOCKS per KTD-4**: document the new op `enumerate_plan_units(ledger) → list[unit_dict]` that both `ce` and `native` adapters implement — it is the producer the emitters read; document the V1 serialized-plan-advance discipline (`next_plan_step` signature preserved); flag the planned v0.3.0 re-lock to add a `unit_id` parameter for concurrent advance)
  - Modify: `.claude-plugin/plugin.json` (version bump 0.1.1 → 0.2.0)
  - Create: `docs/contracts/recipe-format.md` (already created in U2; this unit polishes)

  **Approach:**
  - `README.md` gets a "Recipes" section near the top: what they are, how to pick one, how to author one.
  - Schema doc additive extensions documented with the same prose pattern as the `attempt` field.
  - Version bump matches the marketplace re-sync (deferred follow-up).

  **Execution note:** None.

  **Patterns to follow:**
  - `attempt` field additive precedent.
  - Existing README structure.

  **Test scenarios:**
  - None — docs.
  - Test expectation: none — pure documentation and version metadata.

  **Verification:**
  - `bash tests/run.sh all` still green (the smoke tests verify command→script existence; nothing here moves those).
  - `.claude-plugin/plugin.json` version is `0.2.0`.

---

- U12. **Smart-entry: bare `/auto` gathers context and determines where to pick up**

  **Goal:** Make bare `/auto` (no args) **context-aware** — the orchestrator orients itself and resumes-or-starts intelligently instead of forcing the operator to choose the right verb (`/auto` start vs `/auto-resume` continue vs `--recipe`). *(Added 2026-05-25 from field feedback — see memory `feedback_auto_should_be_context_aware_smart_entry`, the unifying ask behind all four `/auto` operator-UX feedback pieces this session.)*

  **Requirements:** R7 (picker entry), KTD-15 (work-only is the green-plan resume target); field-feedback-driven.

  **Dependencies:** U3 (registry — to detect available recipes + resolve work-only), U4 (`--recipe` wiring — smart-entry routes INTO it), U8 (picker — smart-entry falls through to the picker when it can't auto-determine). **File overlap: U12 and U8 both edit `commands/auto.md` — build U8 first, then U12 layers on top; NOT parallel.**

  **Files:**
  - Modify: `commands/auto.md` (NL routing prose — the smart-entry decision tree, layered above U8's picker logic)
  - Test: `tests/integration/recipe-smart-entry.test.sh` (the decision-tree branches, driven through the lib helpers the prose calls)

  **Approach:** Bare `/auto` runs a context-gather decision tree in the `commands/auto.md` NL-routing prose (orchestrator-layer per `feedback_slash_command_nl_routing_pattern` — `lib/*.sh` stays headless):
  1. **In-flight run?** (an `.claude/auto/<run>.json` exists with a non-terminal `exit_predicate_result.met`) → resume it at its current phase (the `/auto-resume continue` path, auto-chosen). Surface "resuming run X at phase Y" so it's visible.
  2. **No in-flight run, but a reviewed plan present?** (a plan file in `docs/plans/` or a path the operator names) → offer/start the **work-only (W) recipe** (skip the already-done plan-loop, `enumerate_plan_units` → work units). This is the green-plan path the feedback wanted.
  3. **No plan, raw issues/spec?** → recommend `/ce-plan` first (plan-production is UPSTREAM of `/auto`'s work-loop — the third feedback piece), OR offer the A1 plan-loop if the operator wants the engine to drive planning.
  4. **Ambiguous / multiple candidates?** → fall through to U8's picker (which already lists recipes + recent runs).
  - **Prepare/execute affordance** (first feedback piece): whenever smart-entry starts/resumes, the emitted INTENT must state the prepare/execute contract plainly ("run this invocation, feed results back; do NOT loop tick.sh") and, at `plan_step==review_plan` with `gaps_open` still null, warn about the deepen↔review livelock.

  **Execution note:** Pure orchestrator-prose + a thin context-detect lib helper (e.g. `lib/auto-detect.sh` calling a `lib/recipes.py`/`lib/auto.py` "what's my situation" function) if the detection needs deterministic logic. Keep the `$ARGUMENTS`-uniqueness smoke invariant intact (only one `$ARGUMENTS` line in `commands/auto.md`, the dispatch line).

  **Test scenarios:**
  - In-flight non-terminal run present → smart-entry resolves to "resume run X" (not "start new").
  - No run + reviewed plan present → resolves to work-only start.
  - No run + no plan → resolves to "recommend /ce-plan" (does NOT silently start an empty run).
  - Ambiguous (2 in-flight runs, or run + plan) → falls through to the picker.
  - Smoke: `commands/auto.md` still has exactly one `$ARGUMENTS` line (the dispatch line) after U12's prose additions.

  **Verification:**
  - Bare `/auto` in a repo with an in-flight run resumes it; in a repo with only a reviewed plan offers work-only; in a repo with neither recommends `/ce-plan`.
  - Smoke test green (the `$ARGUMENTS`-uniqueness invariant holds).

---

## System-Wide Impact

- **Interaction graph:** Adding recipes touches the engine's loop construction (`init_ledger`), tick loop (`_tick_body`, the former `_maybe_seam` now `_advance_phase_if_met`, `advance_plan_loop`), predicate evaluation (`recompute_predicate` — keeps a SINGLE phase-aware `met` field per KTD-8; the two-field `phase_met` carving is deferred to v0.2.1 with A3), the new `transition_and_emit` ledger primitive (KTD-6), the re-locked adapter contract's new `enumerate_plan_units` op (KTD-4), status reporting (`auto-status`), and resume (`auto-resume`). All except `auto-resume` read the new ledger fields directly. `auto-resume` re-reads the ledger fresh on suspend recovery, so it picks up recipe state automatically — no change needed.

- **Error propagation:** Recipe validation errors at engine load (`load_and_validate`) propagate to `lib/auto.py::run` which exits 1 with a clear message. Recipe validation errors at skill-write propagate to the skill which surfaces them to the user and refuses to write. The contract is `validate` (hard errors, used by both engine and skill) + `validate_and_lint` (additional editorial checks, skill only) per KTD-2. Same hard-error shape across both callers; lint warnings are skill-only and never reject a contract-valid recipe.

- **State lifecycle risks:** A v0.1.x ledger MUST be readable as a v0.2.0 ledger (additive-extension invariant). The R13 regression test enforces this. A run started under v0.2.0 with a workspace recipe survives the recipe file being deleted mid-run — `auto-resume` reads the ledger, not the recipe file, so topology info carries forward via the ledger's `recipe`/`phase_order`/`terminal_phase` fields (KTD-12). New atomic-ordering invariant (G3): the `transition_and_emit` primitive (KTD-6) writes new pending units in the same atomic snapshot as the verdict-observation that triggers it; because emit + recompute + write all happen in one `_with_locked_ledger` body, a crash between transition and emission is impossible by construction.

- **API surface parity:** `--recipe` is a new CLI flag. The `--adapter` flag is orthogonal and unchanged. The picker is a new UX surface on `commands/auto.md`. The authoring skill is a new skill. No existing command, flag, or behavior is removed or changed.

- **Integration coverage:** AE tests distributed across owning units (AE1+AE2 → U8, AE3 → U9, AE4 → U5b, plus the new A4 runtime end-to-end test → U5b). AE5 (A3) is deferred to v0.2.1. Each AE has a deliberate-fail check verified via the Edit tool. The three built-in recipes (A1/A2/A4) are the validator's conformance corpus. The R13 regression test (A1 ≡ v0.1.1) is the recipe-pipeline-wired-correctly smoke test. U10 is the verification gate, SPLIT per phase (each phase audits its own deliberate-fail checks per SG-2) so each phase lands as an audited, independently-meaningful release point.

- **Unchanged invariants:**
  - `dispatch_batch`, `ready_units`, `_is_ready`, `_dependency_satisfied`, `unit_is_terminal`, `record_verdict`, `transition`, `_atomic_write`, `_with_locked_ledger`, `advance_work_loop`, `_ready_fix_unit`, `_ready_reenqueue_unit`, `detect_and_halt_stalled` — all topology-agnostic, all unchanged.
  - The adapter contract (`docs/contracts/adapter-contract.md`) RE-LOCKS in V1 (KTD-4): the existing op signatures (incl. `next_plan_step`) are preserved, but the contract ADDS one new op `enumerate_plan_units(ledger) → list[unit_dict]` (the producer the emitters read), implemented by both `ce` and `native`. Concurrent plan advance (a `unit_id` parameter on `next_plan_step`) stays deferred to v0.3.0. (This is NOT an "unchanged" invariant — it is a deliberate additive re-lock; listed here for completeness against the round-1 "unchanged" framing it corrects.)
  - The smoke-test `$ARGUMENTS`-uniqueness invariant on `commands/*.md` — unchanged. Picker logic adds prose only.
  - The state grammar (`pending → dispatched → ... → terminal-*`) — unchanged.
  - The I-1 atomic predicate freshness invariant — preserved AND extended (G3 corollary: emit-before-recompute inside the same atomic `transition_and_emit` write).

---

## Risks & Dependencies

| Risk | Mitigation |
|------|------------|
| **Plan documents R11/R12 behavior the code doesn't fully wire** (dominant build-bug class from v0.1.1) | Centralized `lib/phase-grammar.py` helper as single decision point; AST lint test enforces no literal `loop_phase` reads outside the helper (KTD-3 — mechanical, not convention); three deliberate-fail tests in U5 each break a different drift mode. |
| **G1 — Dynamic unit emission silently broken** (A2/A4 ship as validating JSON but units never appear at runtime) | Explicit 3-emitter named-function registry in `lib/emitters.py` (U5b, KTD-5); the producer is the new `enumerate_plan_units` adapter op (KTD-4) so the emitters have source data; each emitter has unit tests + at least one edge case; AE4 + the new A4 runtime test assert the dynamic-emission path produces work units; atomic-ordering deliberate-fail confirms emit-then-recompute is correct. |
| **G2 — Predicate semantic conflation** (a non-default-phase recipe could silent-exit because `met` is overloaded for both phase-advance and run-exit) | The two-field carving (`met` + `phase_met`) is DEFERRED to v0.2.1 with A3 (the only recipe that needed it — KTD-8/KTD-14). In V1, `met` stays SINGLE-field but is phase-aware via the phase-grammar helper; since all V1 recipes (A1/A2/A4) use the default `phase_order`/`terminal_phase`, the conflation cannot occur (you can only exit from the terminal phase, which for all V1 recipes is `"work"`). v0.2.1 re-introduces the carving alongside A3's non-default phases. |
| **G3 — Predicate fires before dynamic units emit** (A2 judge terminal → ledger has zero work units → `all_units_terminal==true` → silent exit) | The new `ledger.transition_and_emit` primitive (KTD-6) + I-1 corollary: phase-transition emission writes new pending units in the same atomic snapshot as the triggering verdict-observation. Order inside the ONE locked body: (1) apply transition, (2) run (pure) emitter, (3) append units, (4) recompute predicate, (5) atomic write. Deliberate-fail test reverses recompute and emit, asserts AE4 RED. |
| **G4 — Scalar mirror under concurrent N>1 plan advance is structurally broken** | Resolved by KTD-4: plan-loop units in N>1 mode execute SERIALIZED (round-robin one-at-a-time per tick, tiebroken by per-unit `last_advanced_at`). The `next_plan_step` signature is preserved; the contract re-locks only to ADD `enumerate_plan_units`. Concurrent advance (a `unit_id` parameter on `next_plan_step`) deferred to v0.3.0. |
| **Re-entrancy deadlock if an emitter mutates the ledger** (KTD-6: the seam-handler already holds the lock; `_flock_run` opens a new fd per call) | Emitters are PURE functions (signature `emit(ledger, triggering_unit) → list[new_unit_dict]`) — they MUST NOT call `record_verdict`/`transition`/`set_loop`. The constraint is documented as `lib/emitters.py` module docstrings + the `transition_and_emit` docstring; a unit test asserts an emitter that calls a mutator deadlocks/errors (load-bearing, not convention). |
| **AskUserQuestion preview field doesn't render multi-line ASCII** | V1 fallback is short labels + descriptions; multi-line ASCII is an upgrade attempted in implementation; if it doesn't render, ship the fallback. Both paths tested in U8. |
| **Three independent ASCII renderers drift** (picker, skill, status) | Single renderer at `lib/topology-render.py` (KTD-10, U7); called from all three surfaces. Visual-vocabulary doc is the renderer's contract, not a separate spec. |
| **Authoring skill and engine loader validate differently** | `validate` is the shared contract (called by both); `validate_and_lint` is the skill-side editorial layer that adds non-blocking warnings (KTD-2). Test asserts skill output that passes `validate` is engine-acceptable. |
| **A1 recipe diverges from v0.1.1 behavior** | R13 regression test ("first-tick ledger from `--recipe a1` ≡ first-tick ledger from v0.1.1 invocation, modulo additive fields") is the wiring-correctness smoke test. Run on every PR. Per KTD-1, A1's canonical source is the `A1_BUILTIN` Python constant (NOT a removed legacy path) — a unit test asserts `A1_BUILTIN` equals the resolved `recipes/a1.json` so the two sources can't drift, and the constant is the runtime fallback so built-in install corruption can't break bare `/auto`. |
| **Round-1 fixes only the cited site (narrowing class)** | AST lint test (KTD-3) enforces structural single-source-of-truth mechanically; review-loop budget allocated for ≥3 rounds per institutional learning. |
| **Backward-compat breaks on a v0.1.x ledger** | U1 test loads a pinned v0.1.x fixture and asserts predicate evaluation matches v0.1.1's computation. Fail-stop if it diverges. |
| **Marketplace re-sync forgotten** | Listed under Deferred to Follow-Up Work; surface in the post-merge checklist. |

---

## Documentation / Operational Notes

- **`docs/contracts/recipe-format.md`** is the new LOCKED day-zero contract for v0.2.0. Authoring skill, engine loader, and future recipe authors all read this. Re-locks (incompatible recipe format changes) require all consumers.
- **`docs/contracts/ledger-schema.md`** gets additive extensions per the `attempt` field precedent.
- **`README.md`** gets a Recipes section explaining the picker + the three built-ins (A1/A2/A4; A3 deferred to v0.2.1 per KTD-14) + the authoring skill.
- **Post-merge marketplace re-sync:** Per global CLAUDE.md, the marketplace vendoring is manual. Process: clone `shrimpshack`, branch, rsync `~/projects/auto/` → `plugins/auto/`, bump marketplace.json's auto entry to v0.2.0, PR + merge. This is the follow-up that ships v0.2.0 to users.

---

## Phased Delivery

Two phases — engine + format + emitters first, picker UX + skill second. Each phase is an internally-coherent landing point that doesn't ship a half-feature, AND each phase audits its own deliberate-fail checks (the U10 audit is split per phase per scope-guardian SG-2). Per KTD-1, A1's canonical topology is the `A1_BUILTIN` Python constant (NOT a removed legacy path); bare `/auto <plan>` defaults to A1 via the recipe pipeline from Phase 1 onward (falling back to the constant if `recipes/a1.json` is absent), before the picker ships in Phase 2.

### Phase 1 — Engine, format, emitters (lands first)
- U1 (ledger schema extension — additive)
- U2 (recipe format + JSON Schema validator + recipe-format.md contract)
- U3 (loader + 3-tier registry + `unit_for` + `validate_and_lint`)
- U7 (three built-in recipes A1/A2/A4 + `A1_BUILTIN` constant + `lib/topology-render.py`; A3 deferred to v0.2.1)
- U5 (phase-grammar helper + `_bootstrap.py::load_lib_module` shim + AST lint; single phase-aware `met`)
- U5b (3-emitter registry + `transition_and_emit` primitive + `enumerate_plan_units` adapter op + atomic-ordering wire-up)
- U6 (serialized N-parallel plan-loop advance + round-robin tiebreaker + stalled recovery)
- U4 (`--recipe` flag + recipe-pipeline `init_ledger`; A1 is the new default via the recipe pipeline / `A1_BUILTIN` fallback)
- AE4 (in U5b), the A4 runtime end-to-end test (in U5b), R13 regression (in U4) integration tests
- U10 Phase-1 audit pass (audit U1/U2/U3/U5/U5b/U6 deliberate-fail checks; confirm AE4 + A4 runtime + R13 green)

**Landing point:** `--recipe <name>` works end-to-end. Bare `/auto <plan>` runs A1 by default (via the recipe pipeline, with the `A1_BUILTIN` constant fallback — KTD-1). The three built-ins (A1/A2/A4) run via the explicit flag. Picker hasn't shipped yet; users must type `--recipe a2` etc. to pick non-default. AE4 and the A4 runtime test pass; R13 regression passes; all v0.1.x tests pass unchanged; the Phase-1 deliberate-fail audit is complete. (AE5 / A3 is deferred to v0.2.1.)

### Phase 2 — Picker UX + authoring skill + smart-entry + docs
- U8 (picker in commands/auto.md + recipes-list.sh + recipe-picker.test.sh)
- U12 (smart-entry — bare /auto gathers context + resumes/starts; layered on U8's commands/auto.md, built AFTER U8 — shared file, not parallel; + recipe-smart-entry.test.sh)
- U9 (authoring skill + recipe-authoring.test.sh)
- U10 Phase-2 audit pass (audit U9 + U12 deliberate-fail checks; confirm AE1/AE2/AE3 + smart-entry branches green)
- U11 (docs + version bump + adapter-contract re-lock doc; NO emitter-registry.md per SG-1)

**Landing point:** Bare `/auto` gathers context and picks up (resume in-flight / work-only for a reviewed plan / recommend /ce-plan for raw issues / picker when ambiguous). `/auto-author-recipe` is available. AE1/AE2/AE3 + smart-entry branches pass; the Phase-2 deliberate-fail audit is complete. v0.2.0 is feature-complete (A1/A2/A4/W; A3 ships in v0.2.1).

---

## Sources & References

- **Origin document:** [docs/brainstorms/2026-05-23-auto-v0.2.0-recipes-requirements.md](docs/brainstorms/2026-05-23-auto-v0.2.0-recipes-requirements.md)
- **Locked contracts referenced:** [docs/contracts/ledger-schema.md](docs/contracts/ledger-schema.md), [docs/contracts/adapter-contract.md](docs/contracts/adapter-contract.md)
- **Key institutional learnings:** `feedback_plan_documents_transition_code_doesnt_wire_it`, `feedback_fix_the_class_not_the_cited_instance`, `feedback_slash_command_nl_routing_pattern`, `feedback_loop_monitor_terminal_state_field`, `feedback_new_tests_need_deliberate_fail_smoke_check`, `feedback_deterministic_over_probabilistic_v1`, `reference_claude_loop_task_daemon_primitives` (in `~/.claude/projects/-Users-shawnroos/memory/`)
- **Prior plan that shipped v0.1.1:** None checked in (was the build-and-review loop without a written plan).
