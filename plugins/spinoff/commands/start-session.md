---
description: Spin the current topic/plan/idea off into a fresh worktree + a briefed Claude session in a new tab here
---

The current topic, plan, or idea we've been discussing is becoming its own batch of work. Spin it off into a new **tab** where I am now — herdr, cmux or ghostty, whichever this session is running in.

Invoke the **spinoff** skill and follow it end to end with `--target tab`. The skill owns the workflow — synthesize the handoff, confirm the branch base (here, before backgrounding), resolve this session's transcript, dispatch a background agent to run the spinoff script, and relay the result.

Anything I typed after the command is the workstream name/hint — use it to pick the kebab-case feature name and focus the handoff: $ARGUMENTS
